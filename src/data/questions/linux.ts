import { Question } from '../../types';

export const LINUX_QUESTIONS: Question[] = [
  // =========================================================================
  // ============================ JUNIOR LEVEL ===============================
  // =========================================================================
  {
    id: 'linux-4',
    title: 'Что происходит при удалении активного файла командой rm и как вернуть дисковое пространство?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Команда rm удаляет только имя файла из директории (Unlink). Дисковые блоки освобождаются только когда закрыты все файловые дескрипторы (FD) открывших файл процессов.',
    fullAnswer: `При выполнении rm app.log уменьшается счетчик жестких ссылок (Link Count) в Inode. Если процесс (например, Nginx) продолжает держать файл открытым, дескриптор остается активным, и ядро не высвобождает блоки данных на диске.
Симптом: du -sh показывает мало места, а df -h показывает, что диск забит на 100%.

**Как починить:**
1. Найти заблокированные удаленные файлы: lsof | grep deleted или lsof +L1.
2. Перезапустить процесс: systemctl restart nginx (процесс закроет дескриптор).
3. Если нельзя перезапускать сервис: обнулить дескриптор через /proc/<PID>/fd/<FD_NUM> командой > /proc/1234/fd/4.`,
    codeSnippet: {
      language: 'bash',
      code: `# Поиск "удаленных, но занятых" файлов:
sudo lsof / | grep deleted

# Правильное обнуление активного лога БЕЗ rm:
> /var/log/nginx/access.log`
    },
    interviewTips: [
      'Назовите правило: никогда не удалять живые логи через rm, а обнулять их через > file.log или настраивать logrotate.'
    ],
    commonPitfalls: [
      'Удалять открытый огромный лог файл через rm -rf и удивляться, почему свободное место в df -h не изменилось.'
    ],
    tags: ['Linux', 'Filesystem', 'lsof', 'Inodes', 'Troubleshooting']
  },
  {
    id: 'linux-6',
    title: 'Как работают права доступа в Linux (chmod, chown, SUID, SGID, Sticky Bit)?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Стандартные права — Read (4), Write (2), Execute (1) для Owner, Group, Others. Спец-биты: SUID исполняет файл с правами владельца, SGID наследует группу, Sticky Bit запрещает удалять чужие файлы в /tmp.',
    fullAnswer: `1. **chmod 755 / 644**:
   - 7 (rwx) = 4 + 2 + 1
   - 6 (rw-) = 4 + 2
   - 5 (r-x) = 4 + 1

2. **Специальные биты прав (Special Bits)**:
   - **SUID** (chmod 4755 / u+s): При запуск бинарника процесс выполняется от имени владельца файла (например /usr/bin/passwd имеет SUID bit, чтобы обычный юзер мог менять файл /etc/shadow).
   - **SGID** (chmod 2755 / g+s): Для директорий: все новые файлы внутри автоматически наследуют группу родительской папки.
   - **Sticky Bit** (chmod 1777 / +t): Для папок (например /tmp): пользователь может удалять ТОЛЬКО те файлы, владельцем которых он является.`,
    codeSnippet: {
      language: 'bash',
      code: `chmod 1777 /tmp # установить sticky bit
chmod u+s /usr/local/bin/my-tool # установить SUID
ls -la /tmp # выведет drwxrwxrwt (буква t в конце)`
    },
    interviewTips: [
      'SUID на бинарниках — популярный вектор атак Privilege Escalation. Назовите команду find / -perm -4000 для их поиска.'
    ],
    commonPitfalls: [
      'Устанавливать chmod 777 в продакшене "чтобы всё заработало".'
    ],
    tags: ['Linux', 'Security', 'Permissions', 'Chmod', 'SUID']
  },
  {
    id: 'linux-8',
    title: 'Что такое Systemd unit, какие типы юнитов существуют и как ими управлять?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Systemd — подсистема инициализации Linux (PID 1). Unit — конфигурационный файл службы. Типы: .service, .socket, .target, .timer, .mount.',
    fullAnswer: `Ключевые типы юнитов:
1. **.service**: описывает параметры запуска процесса (ExecStart, Restart=always, User, EnvironmentFile).
2. **.target**: группировка юнитов для создания уровней загрузки (analog runlevels): multi-user.target, graphical.target.
3. **.timer**: аналог cron, управляемый systemd с протоколированием в journalctl.
4. **.socket**: активирует сервис по приходу первого сетевого пакета (Socket Activation).

Команды управления:
- systemctl start/stop/restart/status <unit>
- systemctl enable/disable <unit> (создает/удаляет симлинки в /etc/systemd/system/multi-user.target.wants/)
- systemctl daemon-reload (обязательно после изменения файла юнита!).`,
    codeSnippet: {
      language: 'ini',
      code: `[Unit]
Description=My Go Application
After=network.target

[Service]
Type=simple
User=appuser
ExecStart=/usr/local/bin/myapp
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target`
    },
    interviewTips: [
      'Не забудьте упомянуть systemctl daemon-reload при правках файлов в /etc/systemd/system/.'
    ],
    commonPitfalls: [
      'Забывать секцию [Install] WantedBy=multi-user.target, без которой systemctl enable не сработает.'
    ],
    tags: ['Linux', 'Systemd', 'Services', 'Init', 'Journalctl']
  },
  {
    id: 'linux-9',
    title: 'В чем разница между жесткими (Hard links) и символическими (Soft/Symlinks) ссылками?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Hard link — дополнительный указатель на тот же Inode на диске. Symlink — отдельный файл, содержащий путь к целевому файлу.',
    fullAnswer: `1. **Hard Link (Жесткая ссылка)**:
   - Указывает напрямую на тот же индексный дескриптор (Inode).
   - "Удаление" оригинального файла НЕ приводит к потере данных, пока существует хотя бы одна жесткая ссылка.
   - НЕ МОЖЕТ создавать ссылки на директории и НЕ МОЖЕТ пересекать границы файловых систем.

2. **Symlink (Символическая / Мягкая ссылка)**:
   - Отдельный специальный файл со своим собственным Inode, хранящий текстовую строку с путем к целевому файлу.
   - Если оригинальный файл удален, ссылка становится "битой" (Dangling symlink).
   - Может указывать на директории и на файлы на ДРУГИХ файловых системах.`,
    codeSnippet: {
      language: 'bash',
      code: `ln original.txt hardlink.txt # Жесткая ссылка
ln -s original.txt symlink.txt # Символическая ссылка
ls -i # Посмотреть Inode файлов`
    },
    interviewTips: [
      'Упомяните применение symlink при атомарном переключении версий релизов (например /app/current -> /app/releases/v1.2.3).'
    ],
    commonPitfalls: [
      'Пытаться создать hard link на каталог или между разными смонтированными дисками.'
    ],
    tags: ['Linux', 'Filesystem', 'Inodes', 'Links']
  },
  {
    id: 'linux-15',
    title: 'Что такое ротация логов logrotate и как она предотвращает переполнение диска?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'logrotate — системная утилита для автоматического сжатия, переименования, архивации и удаления устаревших файлов логов на основе размера или возраста.',
    fullAnswer: `Без logrotate логи приложений забивают всё дисковое пространство.
Конфигурация хранится в /etc/logrotate.conf и /etc/logrotate.d/*.

Основные директивы:
- **daily / weekly / monthly**: периодичность ротации лога.
- **rotate 14**: хранить максимум 14 архивных копий.
- **compress**: сжимать старые логи через gzip.
- **missingok**: не выдавать ошибку, если файла лога нет.
- **copytruncate**: скопировать содержимое лога в архив и обнулить текущий файл на лету БЕЗ перезапуска приложения!`,
    codeSnippet: {
      language: 'ini',
      code: `/var/log/myapp/*.log {
    daily
    missingok
    rotate 14
    compress
    delaycompress
    notifempty
    copytruncate
}`
    },
    interviewTips: [
      'Директива copytruncate — спасение для приложений, которые не умеют переоткрывать дескриптор файла по сигналу SIGHUP.'
    ],
    commonPitfalls: [
      'Забывать настроить logrotate для кастомных логов в /var/log/.'
    ],
    tags: ['Linux', 'Logrotate', 'Logging', 'Maintenance']
  },
  {
    id: 'linux-16',
    title: 'В чем разница при поиске файлов между командами find и locate?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'find производит поиск в режиме реального времени на диске (медленно, но всегда актуально). locate осуществляет поиск по заранее проиндексированной базе данных mlocate (очень быстро, но база может быть устаревшей).',
    fullAnswer: `1. **find**:
   - Сканирует реальную файловую структуру на диске при каждом запуске.
   - Поддерживает гибкие фильтры: поиск по типу, правам доступа, владельцу, размеру, дате модификации.
   - Позволяет сразу выполнять команды над найденными файлами через опцию -exec.
   
2. **locate**:
   - Работает за миллисекунды, так как ищет только совпадение имен файлов по встроенной базе данных (/var/lib/mlocate/mlocate.db).
   - База данных по умолчанию обновляется раз в сутки через cron.
   - Для принудительного обновления базы используется команда updatedb.`,
    codeSnippet: {
      language: 'bash',
      code: `# Поиск файлов логов больше 100МБ и их удаление с подтверждением
find /var/log -type f -name "*.log" -size +100M -ok rm {} \\;

# Мгновенный поиск файлов по имени во всей системе
sudo updatedb && locate nginx.conf`
    },
    interviewTips: [
      'find — мощный швейцарский нож. Выучите синтаксис find /path -type f -mtime +30 -delete для удаления файлов старше 30 дней.'
    ],
    commonPitfalls: [
      'Пытаться использовать locate для поиска только что созданного файла, забыв выполнить updatedb.'
    ],
    tags: ['Linux', 'CLI', 'Filesystem', 'Search']
  },
  {
    id: 'linux-17',
    title: 'Как посмотреть список запущенных процессов и разницу между командами ps, top, htop?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'ps выдает статический снимок активных процессов на текущий момент. top и htop — интерактивные диспетчеры задач реального времени. htop отличается наглядностью, поддержкой мыши и древовидным списком.',
    fullAnswer: `1. **ps (process status)**:
   - Делает снимок в момент вызова. Популярные ключи: ps aux (вывести процессы всех пользователей, в формате с указанием владельца) или ps -ef (стандартный синтаксис System V).
   - Позволяет легко грепать процессы: ps aux | grep gunicorn.

2. **top**:
   - Стандартный терминальный монитор в реальном времени. Входит в состав любого дистрибутива.
   - Не имеет удобной графики, цвета и сортировки по умолчанию (нужно настраивать горячими клавишами M/P/T).

3. **htop**:
   - Современная альтернатива top.
   - Отображает графические шкалы нагрузки CPU по ядрам, использование памяти и swap.
   - Позволяет легко убивать процессы (F9), искать (F3), фильтровать (F4) и раскрывать дерево зависимостей (F5).`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть подробную информацию о процессе по имени:
ps -ef | grep postgres

# Интерактивный монитор:
htop`
    },
    interviewTips: [
      'Полезно упомянуть ключ "axjf" в ps для построения дерева процессов в стандартном выводе.'
    ],
    commonPitfalls: [
      'Пытаться установить htop на продакшене без согласования, хотя в системе гарантированно есть дефолтный top.'
    ],
    tags: ['Linux', 'Processes', 'CLI', 'Monitoring']
  },
  {
    id: 'linux-18',
    title: 'Как отфильтровать вывод команды или лог-файла с помощью утилит grep, egrep, fgrep?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'grep ищет текстовые строки по регулярным выражениям. egrep — расширенная версия (grep -E). fgrep — быстрый поиск фиксированных строк без поддержки регулярок (grep -F).',
    fullAnswer: `1. **grep**: По умолчанию использует базовые регулярные выражения (BRE). Чтобы экранировать спецсимволы вроде |, +, ?, их приходится писать со слэшем.
2. **egrep (Extended grep)**: Поддерживает расширенные регулярные выражения (ERE). Символы |, +, ?, {, } работают без экранирования.
3. **fgrep (Fixed grep)**: Игнорирует все спецсимволы регулярных выражений, воспринимая их как обычный текст. Работает быстрее всех, незаменим для поиска конкретных IP-адресов или путей файлов.

**Часто используемые флаги:**
- -i: игнорировать регистр.
- -v: инвертировать поиск (выводить строки, где совпадения НЕТ).
- -c: вывести только количество найденных строк.
- -r или -R: рекурсивный поиск в директории.
- -n: выводить номера строк.
- -A <num> / -B <num>: вывести строки после/до совпадения.`,
    codeSnippet: {
      language: 'bash',
      code: `# Найти ошибки 500/502/504 в логах Nginx (через egrep/grep -E)
grep -E ' 50[024] ' /var/log/nginx/access.log

# Найти конкретную строку без парсинга регулярок
fgrep '192.168.1.1' /var/log/syslog`
    },
    interviewTips: [
      'Используйте grep -rnw "/path" -e "pattern" для рекурсивного поиска точного слова во всей директории.'
    ],
    commonPitfalls: [
      'Использовать обычный grep и регулярку с символом |, удивляясь, почему ничего не находится без экранирования.'
    ],
    tags: ['Linux', 'CLI', 'Grep', 'TextProcessing']
  },
  {
    id: 'linux-19',
    title: 'Как работают пакетные менеджеры в Linux (apt, yum, dnf) и что такое репозиторий?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Пакетный менеджер устанавливает, обновляет и удаляет программное обеспечение, разрешая зависимости. Репозиторий — удаленный сервер-хранилище скомпилированных пакетов и метаданных.',
    fullAnswer: `Пакетные менеджеры избавляют от необходимости компилировать софт из исходников вручную. Они считывают зависимости (библиотеки) и скачивают их вместе с целевым ПО.

Разделение по семействам дистрибутивов:
1. **Debian/Ubuntu (пакеты .deb)**:
   - Низкоуровневая утилита: dpkg.
   - Высокоуровневая утилита: apt (ранее apt-get).
   - Файлы репозиториев: /etc/apt/sources.list и /etc/apt/sources.list.d/*.
   
2. **RedHat/CentOS/Rocky Linux (пакеты .rpm)**:
   - Низкоуровневая утилита: rpm.
   - Высокоуровневая утилита: dnf (ранее yum).
   - Файлы репозиториев: /etc/yum.repos.d/*.conf.

Основные команды:
- apt update / dnf check-update — скачивание свежих метаданных репозиториев.
- apt upgrade / dnf upgrade — обновление установленных пакетов.
- apt install <pkg> / dnf install <pkg> — установка ПО.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть, какому пакету принадлежит файл /etc/nginx/nginx.conf
# На Debian/Ubuntu:
dpkg -S /etc/nginx/nginx.conf

# На RHEL/CentOS:
rpm -qf /etc/nginx/nginx.conf`
    },
    interviewTips: [
      'Объясните разницу между update (обновление списков доступных пакетов) и upgrade (непосредственная установка новых версий пакетов).'
    ],
    commonPitfalls: [
      'Запускать apt install без предварительного apt update, что приводит к ошибкам "Package not found" из-за устаревших кэшей.'
    ],
    tags: ['Linux', 'Packages', 'Ubuntu', 'Debian', 'CentOS']
  },
  {
    id: 'linux-20',
    title: 'В чем разница между временными и постоянными переменными окружения (.bashrc, .profile)?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Временные переменные действуют только в текущей сессии терминала. Постоянные прописываются в конфигурационных файлах пользователя (.bashrc, .bash_profile) или системы (/etc/environment).',
    fullAnswer: `Переменные окружения (Environment Variables) задают конфигурацию приложений в ОС.

1. **Временные (в рамках shell-сессии)**:
   - Создаются через MY_VAR="value".
   - Чтобы передать переменную дочерним процессам текущего шелла, ее нужно экспортировать: export MY_VAR="value".
   - При закрытии терминала переменная уничтожается.

2. **Постоянные**:
   - **.bashrc**: Читается при каждом запуске интерактивного non-login shell (открытие нового окна терминала в GUI). Подходит для alias и локальных переменных окружения.
   - **.bash_profile / .profile**: Читается ОДИН раз при входе в систему (login shell), например при подключении по SSH.
   - **/etc/environment**: Системный файл, не поддерживающий shell-скрипты. Хранит переменные в формате KEY=VAL для всех пользователей.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть все переменные окружения текущего пользователя
env

# Экспортировать переменную на постоянной основе для текущего пользователя:
echo 'export DATABASE_URL="postgres://localhost:5432/db"' >> ~/.bashrc
source ~/.bashrc`
    },
    interviewTips: [
      'Упомяните утилиту source (или просто .), которая позволяет применить изменения из .bashrc в текущую сессию терминала без перелогина.'
    ],
    commonPitfalls: [
      'Забыть написать export при объявлении переменной в терминале, из-за чего запущенный следом скрипт python/node не увидит эту переменную.'
    ],
    tags: ['Linux', 'Bash', 'CLI', 'Shell']
  },
  {
    id: 'linux-21',
    title: 'Направление потоков ввода-вывода: что значат символы >, >>, 2>, 2>&1?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Символы перенаправляют стандартные потоки данных. > перезаписывает файл, >> дописывает в конец. 2> перенаправляет ошибки (stderr). 2>&1 объединяет stderr с stdout.',
    fullAnswer: `В Linux любой процесс имеет три стандартных потока ввода-вывода, представленных дескрипторами:
- **0** — stdin (стандартный ввод).
- **1** — stdout (стандартный вывод).
- **2** — stderr (стандартный вывод ошибок).

Синтаксис перенаправления:
- \`command > file.txt\`: Перенаправить stdout в файл, затерев его содержимое (эквивалентно \`1>\`).
- \`command >> file.txt\`: Дописать stdout в конец файла.
- \`command 2> error.log\`: Направить только ошибки (stderr) в файл.
- \`command > output.log 2>&1\`: Направить stdout в output.log, а stderr (2) направить туда же, куда указывает stdout (1).
- \`command &> file.txt\`: Современный синтаксис Bash для перенаправления и stdout, и stderr в один файл.`,
    codeSnippet: {
      language: 'bash',
      code: `# Запуск фонового скрипта с полным глушением вывода (в черную дыру)
./background_job.sh > /dev/null 2>&1 &

# Запись логов ошибок в один файл, а обычных логов в другой
./app > info.log 2> error.log`
    },
    interviewTips: [
      'Объясните концепцию специального пустого устройства /dev/null, используемого для утилизации ненужного вывода.'
    ],
    commonPitfalls: [
      'Использовать > вместо >>, что случайно уничтожает исторический лог-файл при записи нового лога.'
    ],
    tags: ['Linux', 'Bash', 'CLI', 'Streams']
  },
  {
    id: 'linux-22',
    title: 'Как запаковать и сжать файлы с помощью утилит tar, gzip и zip?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'tar собирает множество файлов в один архив без сжатия (tarball). gzip сжимает один файл. Их комбинация создает сжатые архивы .tar.gz. zip выполняет и упаковку, и сжатие по умолчанию.',
    fullAnswer: `В мире Linux разделены понятия архивации (объединения файлов) и компрессии (сжатия данных).

1. **tar (Tape Archive)**:
   - Объединяет дерево папок в один файл с сохранением прав доступа, владельцев и структуры каталогов.
   - Ключевые флаги:
     - -c: create (создать архив).
     - -x: extract (распаковать).
     - -v: verbose (показывать список обрабатываемых файлов).
     - -f: file (указать имя архива).
     - -z: использовать сжатие gzip (.tar.gz).
     - -j: использовать сжатие bzip2 (.tar.bz2).

2. **gzip**:
   - Сжимает только одиночный файл, заменяя его версией .gz. Не умеет упаковывать папки!

3. **zip**:
   - Стандартный формат для совместимости с Windows. Упаковывает и сжимает папки рекурсивно при помощи ключа -r.`,
    codeSnippet: {
      language: 'bash',
      code: `# Создать сжатый архив папки с исходниками
tar -czvf project_backup.tar.gz /var/www/myproject

# Распаковать архив tar.gz в текущую директорию
tar -xzvf project_backup.tar.gz`
    },
    interviewTips: [
      'Запомните мнемоническое правило для создания архива: "create-zip-verbose-file" -> czvf, и распаковки: "extract-zip-verbose-file" -> xzvf.'
    ],
    commonPitfalls: [
      'Пытаться применить gzip к папке напрямую. Программа выдаст предупреждение, что это директория, и проигнорирует действие.'
    ],
    tags: ['Linux', 'CLI', 'Archiving', 'Backup']
  },
  {
    id: 'linux-23',
    title: 'Как управлять учетными записями пользователей и группами в Linux (useradd, usermod, groupadd)?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Пользователи создаются через useradd, изменяются через usermod. Группы создаются через groupadd. Конфигурация хранится в файлах /etc/passwd, /etc/shadow и /etc/group.',
    fullAnswer: `1. **/etc/passwd**: содержит список пользователей, их ID (UID), дефолтную группу (GID), домашнюю директорию и shell (/bin/bash или /sbin/nologin для системных юзеров).
2. **/etc/shadow**: хранит зашифрованные хэши паролей и параметры их истечения. Доступен только root.
3. **/etc/group**: содержит список групп и входящих в них пользователей.

Основные команды:
- \`useradd -m -s /bin/bash appuser\`: создать пользователя с домашней папкой (-m) и оболочкой bash.
- \`passwd appuser\`: задать пароль пользователю.
- \`usermod -aG docker appuser\`: добавить пользователя во вторичную группу docker (флаг -a важен для добавления без удаления из текущих групп).
- \`userdel -r appuser\`: удалить пользователя вместе с домашней директорией (-r).`,
    codeSnippet: {
      language: 'bash',
      code: `# Добавить пользователя в группу администраторов sudo на Ubuntu
sudo usermod -aG sudo deployer

# Заблокировать учетную запись пользователя
sudo usermod -L baduser`
    },
    interviewTips: [
      'Всегда используйте флаг -aG при добавлении в группу. Если написать просто -G, пользователь удалится из всех остальных дополнительных групп, в которых состоял!'
    ],
    commonPitfalls: [
      'Забыть поставить флаг -a при вызове usermod -G, что сбросит все предыдущие группы пользователя (включая доступ к sudo).'
    ],
    tags: ['Linux', 'Security', 'UserManagement', 'CLI']
  },
  {
    id: 'linux-24',
    title: 'В чем разница между выводом команд df -h и du -sh при анализе диска?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'df -h показывает свободное и занятое пространство на всех смонтированных файловых системах. du -sh вычисляет размер конкретной папки или файла рекурсивно на диске.',
    fullAnswer: `Обе утилиты служат для мониторинга дискового пространства, но подходят для разных задач:

1. **df (disk free)**:
   - Читает метаданные суперблока файловых систем. Работает мгновенно.
   - Показывает картину целиком: смонтированные разделы, диски, размер, процент использования и точки монтирования.
   - Опция -h выводит данные в человекочитаемом формате (GB, MB).
   - Опция -i выводит занятость Inodes.

2. **du (disk usage)**:
   - Обходит всё дерево файлов и каталогов по указанному пути, складывая размеры файлов. На больших дисках может работать долго.
   - Используется для поиска конкретной папки, забившей диск.
   - Параметр -s (summary) предотвращает вывод каждого файла, показывая только суммарный размер папки.`,
    codeSnippet: {
      language: 'bash',
      code: `# Оценить общую ситуацию по дискам
df -h

# Найти топ-10 самых тяжелых папок в каталоге /var
sudo du -sh /var/* | sort -rh | head -n 10`
    },
    interviewTips: [
      'Если df показывает 100% утилизацию, а du в корне / не находит тяжелых файлов — проверьте удаленные, но занятые процессами файлы через lsof.'
    ],
    commonPitfalls: [
      'Запускать du -sh / на терабайтном диске в продакшене во время пиковой нагрузки (вызовет серьезный I/O оверхед).'
    ],
    tags: ['Linux', 'Filesystem', 'DiskUsage', 'Troubleshooting']
  },
  {
    id: 'linux-25',
    title: 'Проверка сетевого соединения: как работают ping, curl и nc (netcat)?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'ping проверяет доступность хоста на уровне L3 по протоколу ICMP. curl проверяет прикладной уровень (HTTP, FTP и др.). nc (netcat) проверяет доступность конкретного TCP/UDP порта (уровень L4).',
    fullAnswer: `Сетевая отладка в Linux строится снизу вверх по модели OSI:

1. **ping (ICMP)**:
   - Отправляет пакеты ICMP Echo Request.
   - Помогает проверить базовую связность сети и задержку (RTT).
   - *Нюанс*: Фаерволы многих серверов блокируют ICMP (ping не проходит, хотя сайт работает).

2. **nc / netcat (TCP/UDP)**:
   - "Швейцарский армейский нож" для работы с сокетами.
   - Ключ -z запускает сканирование портов без отправки данных.
   - Позволяет мгновенно проверить, открыт ли порт на удаленном сервере (например, база данных).

3. **curl (HTTP/HTTPS/API)**:
   - Полноценный консольный клиент. Проверяет заголовки, SSL-сертификаты, коды ответов (200, 403, 500) и тело ответа.`,
    codeSnippet: {
      language: 'bash',
      code: `# Проверить, доступен ли порт СУБД PostgreSQL на удаленном сервере
nc -zvw3 10.0.0.5 5432

# Сделать HTTP-запрос и вывести только HTTP-код ответа
curl -s -o /dev/null -w "%{http_code}\\n" https://google.com`
    },
    interviewTips: [
      'Умение использовать nc -zv для проверки портов вместо тяжелого nmap — отличный навык для быстрого траблшутинга сетевых доступов.'
    ],
    commonPitfalls: [
      'Верить, что если ping не идет, то сервер выключен. Администраторы часто закрывают ICMP в настройках iptables.'
    ],
    tags: ['Linux', 'Networking', 'Troubleshooting', 'CLI']
  },
  {
    id: 'linux-26',
    title: 'В чем отличие использования команд cat, less, tail -f и head при чтении файлов?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'cat выливает весь файл в терминал разом. less открывает файл с постраничной навигацией и поиском. tail -f выводит конец файла и динамически стримит новые строки лога. head выводит первые строки файла.',
    fullAnswer: `Выбор утилиты для чтения критически зависит от размера файла:

1. **cat**:
   - Читает файл от начала до конца и выводит всё в stdout.
   - Использовать только для маленьких файлов или для конкатенации/перенаправления в другие утилиты.
   - **Опасно** открывать логи размером в гигабайты через cat — это заморозит ваш терминал.

2. **less**:
   - Открывает файл в режиме пейджера. Читает файл лениво (кусками), не загружая весь гигабайт в оперативную память.
   - Поддерживает перемещение вверх/вниз, поиск по регулярным выражениям через кнопку \`/\`.

3. **tail -f / tail -F**:
   - Выводит последние N строк файла (по умолчанию 10).
   - Флаг -f (follow) держит дескриптор открытым и дописывает новые строки в консоль по мере их появления.
   - Флаг -F отслеживает файл даже при его пересоздании (ротации логов).

4. **head**:
   - Выводит первые N строк файла. Используется для просмотра заголовков CSV или конфигураций.`,
    codeSnippet: {
      language: 'bash',
      code: `# Динамический мониторинг лога ошибок nginx
tail -n 50 -F /var/log/nginx/error.log

# Постраничный просмотр тяжелого файла конфигурации
less /etc/postgresql/15/main/postgresql.conf`
    },
    interviewTips: [
      'Знание разницы между tail -f и tail -F (который умеет переоткрывать файл после ротации) показывает практический опыт траблшутинга.'
    ],
    commonPitfalls: [
      'Запуск cat huge_production_access.log, парализующий терминал из-за огромного объема данных.'
    ],
    tags: ['Linux', 'CLI', 'TextProcessing', 'Troubleshooting']
  },
  {
    id: 'linux-27',
    title: 'Как планировать периодические задачи в Linux с помощью Cron (crontab)?',
    category: 'linux',
    difficulty: 'Junior',
    summaryAnswer: 'Cron — демон-планировщик. Настройка задач пользователя производится через команду crontab -e. Формат записи состоит из 5 полей времени (минута, час, день, месяц, день недели) и пути к скрипту.',
    fullAnswer: `Для планирования фоновых задач в Linux используется служба cron.

Синтаксис строки crontab (5 звездочек):
\`* * * * * /path/to/script.sh\`
1. Минута (0 - 59)
2. Час (0 - 23)
3. День месяца (1 - 31)
4. Месяц (1 - 12)
5. День недели (0 - 6, где 0 и 7 — воскресенье)

Специальные символы:
- \`*\`: любое значение.
- \`/\`: шаг/интервал (например \`*/5\` — каждые 5 минут).
- \`,\`: перечисление (например \`1,15,30\` — в эти минуты).
- \`-\`: диапазон (например \`1-5\` — с понедельника по пятницу).

Команды управления:
- \`crontab -e\`: редактировать файл cron текущего пользователя.
- \`crontab -l\`: вывести список текущих задач пользователя.
- \`crontab -r\`: удалить все задачи пользователя.`,
    codeSnippet: {
      language: 'bash',
      code: `# Запись в crontab: Запуск бэкапа базы каждый день в 03:30 ночи
30 3 * * * /usr/local/bin/backup_db.sh >> /var/log/cron_backup.log 2>&1

# Каждые 10 минут с понедельника по пятницу
*/10 * * * 1-5 /usr/local/bin/healthcheck.sh`
    },
    interviewTips: [
      'Назовите главную уязвимость cron-скриптов: при запуске из cron переменная окружения PATH урезана до минимума (/usr/bin:/bin). Всегда пишите полные пути к бинарникам (/usr/local/bin/node вместо node).'
    ],
    commonPitfalls: [
      'Забывать перенаправлять stderr и stdout скрипта в лог-файл, из-за чего cron шлет спам на локальную почту root и скрывает ошибки.'
    ],
    tags: ['Linux', 'Automation', 'Cron', 'Maintenance']
  },

  // =========================================================================
  // ============================ MIDDLE LEVEL ===============================
  // =========================================================================
  {
    id: 'linux-1',
    title: 'Сервер ушел в High Load Average (LA). Как диагностировать причину по шагам?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Load Average показывают среднее число процессов в очередях R (Running) и D (Uninterruptible Sleep / I/O wait). Диагностика: htop, uptime, iostat, vmstat, dmesg.',
    fullAnswer: `1. Посмотреть значения LA за 1, 5 и 15 минут командой uptime или htop. Сравнить с количеством ядер CPU (nproc).
2. Разделить причину:
   - **CPU-bound** (процессы в состоянии R): видно в top / htop по %us, %sy. Показывает, какое приложение нагружает процессор.
   - **I/O-bound** (процессы в состоянии D - Uninterruptible Sleep): высокое значение %wa (iowait) в top. Причина: медленный/забитый диск, проблемы с RAID, NFS.
3. Инструменты утилиты:
   - iostat -xz 1 — нагрузка на дисковую подсистему (%util, await).
   - vmstat 1 — очереди r (run) и b (blocked), swap.
   - pidstat -d 1 / iotop — определение процесса, активно пишущего/читающего диск.
   - dmesg -T | tail -n 50 — проверка аппаратных ошибок дисков или паники ядра.`,
    codeSnippet: {
      language: 'bash',
      code: `uptime
top -b -n 1 | head -n 20
iostat -xz 1 5
vmstat 1 5
ps aux | awk '{if ($8 ~ /D/) print $0}' # поиск процессов в D-state`
    },
    interviewTips: [
      'Четко объясните разницу между процессом в состоянии R (выполняется) и D (ждет ввода-вывода). Это разделяет Джунов от опытных системных инженеров.'
    ],
    commonPitfalls: [
      'Думать, что LA > 1 на 16-ядерном сервере — это катастрофа. Важно соотносить LA с числом логических ядер.'
    ],
    tags: ['Linux', 'Performance', 'Troubleshooting', 'Systemd', 'I/O']
  },
  {
    id: 'linux-3',
    title: 'В чем разница между RSS, VSZ, PSS и Page Cache в оперативной памяти Linux?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'VSZ — запрошенный объем виртуальной памяти. RSS — физическая RAM процесса (включая общие библиотеки). PSS — честная доля физической RAM (Shared деленное на участников). Page Cache — кэш файлов с диска.',
    fullAnswer: `1. **VSZ (Virtual Memory Size)**: Весь объем памяти, который процесс зарезервировал (включая код, библиотеки, невыделенные страницы).
2. **RSS (Resident Set Size)**: Реально выделенная физическая память RAM. Включает общие библиотеки (Shared Libraries). Если 5 процессов используют одну библиотеку в 10 МБ, RSS каждого покажет 10 МБ.
3. **PSS (Proportional Set Size)**: Самая честная метрика! Делит размер общих библиотек на количество процессов, использующих их.
4. **USS (Unique Set Size)**: Объем памяти, принадлежащий СТРОГО данному процессу (при убиении процесса освободится именно USS).
5. **Page Cache**: Свободная оперативная память, используемая ядром для кэширования дискового ввода-вывода (строки buff/cache в free -h). Память из Page Cache высвобождается мгновенно при необходимости приложениям!`,
    codeSnippet: {
      language: 'bash',
      code: `free -h # просмотр свободной памяти и available
smem -r -k # просмотр PSS/USS процессов
cat /proc/meminfo | grep -i dirty # "грязные" неотфлешенные страницы`
    },
    interviewTips: [
      'Объясните, почему free = 300MB при available = 5GB — это абсолютно нормальная работа Linux (Page Cache ускоряет диски).'
    ],
    commonPitfalls: [
      'Паниковать из-за маленького значения free в выводе free -h.'
    ],
    tags: ['Linux', 'Memory', 'RSS', 'PSS', 'PageCache']
  },
  {
    id: 'linux-5',
    title: 'Что такое Inode (индексный дескриптор) и что делать, если иноды закончились?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Inode — структура данных, хранящая метаданные файла (права, размер, владельца, блоки на диске), КРОМЕ его имени. Если иноды закончились, нельзя создать файл даже при наличии свободных гигабайт.',
    fullAnswer: `При форматировании файловой системы (Ext4) создается фиксированное количество Inode. Каждый созданный файл или папка занимает ровно 1 Inode.
Если в системе нагенерировался миллион мелких файлов (например, мелкие сессии PHP, сползшие логи, кэш почты), иноды исчерпываются (df -i показывает 100%).
Приложение выкинет ошибку "No space left on device", хотя df -h покажет терабайты свободной памяти.

**Решение:**
1. Проверить занятость инод: df -i.
2. Найти папку с наибольшим количеством файлов:
   find / -xdev -type d -exec sh -c 'echo "$(find "$1" -type f | wc -l) $1"' _ {} \\; | sort -nr | head -n 10
3. Удалить лишние мелкие файлы или настроить автоматическую ротацию.`,
    codeSnippet: {
      language: 'bash',
      code: `df -i # Проверка инод
find /var/spool/postfix/maildrop -type f -delete # быстрый способ удаления миллионов мелких файлов`
    },
    interviewTips: [
      'Упомяните, что ФС XFS умеет создавать иноды динамически по мере необходимости, в отличие от фиксированной Ext4.'
    ],
    commonPitfalls: [
      'Пытаться удалить миллион файлов обычным rm * (получите ошибку Argument list too long). Нужно использовать find -delete.'
    ],
    tags: ['Linux', 'Inodes', 'Ext4', 'XFS', 'Troubleshooting']
  },
  {
    id: 'linux-7',
    title: 'Как устроена виртуальная файловая система /proc и /sys?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: '/proc и /sys — это псевдо-файловые системы в памяти (procfs и sysfs), предоставляющие динамический интерфейс взаимодействия с ядром и процессами.',
    fullAnswer: `1. ** /proc (procfs)**:
   - Содержит информацию о процессах и параметрах ядра.
   - Папки с числами /proc/<PID> хранят информацию о конкретном процессе: /proc/123/cmdline, /proc/123/environ, /proc/123/fd/ (открытые файловые дескрипторы), /proc/123/status.
   - /proc/sys/ — изменяемые на лету параметры ядра (net.ipv4.ip_forward и т.д.).

2. ** /sys (sysfs)**:
   - Иерархическое представление структуры устройств, драйверов, cgroups и модулей ядра.
   - Используется утилитами udev, lscpu, cgroups v2.`,
    codeSnippet: {
      language: 'bash',
      code: `# Включить ip forwarding в ядре без перезагрузки:
echo 1 > /proc/sys/net/ipv4/ip_forward

# Посмотреть окружение процесса с PID 1024:
cat /proc/1024/environ | tr '\\0' '\\n'`
    },
    interviewTips: [
      'Покажите умение работать с /proc при отсутствии утилит типа ps/top (например в минимальном контейнере без bash).'
    ],
    commonPitfalls: [
      'Пытаться измерить объем /proc утилитой du (получите странные или нулевые размеры, так как это данные из RAM).'
    ],
    tags: ['Linux', 'Procfs', 'Kernel', 'Sysfs', 'Troubleshooting']
  },
  {
    id: 'linux-10',
    title: 'Как работает процесс загрузки Linux (Boot Process) от нажатия кнопки до Bash?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'BIOS/UEFI -> POST -> Bootloader (GRUB) -> Загрузка Ядра (Kernel + initramfs) -> Запуск процесса инициализации PID 1 (Systemd) -> Targets/Userspace.',
    fullAnswer: `1. **BIOS/UEFI**: Инициализирует железо, проходит тест POST и читает MBR/EFI системный раздел.
2. **GRUB (Bootloader)**: Загрузчик передает управление ядру Linux, передавая параметры ядра (/vmlinuz) и RAM-диск первичной инициализации (initramfs).
3. **Kernel Init**: Ядро монтирует временную ФС initramfs, загружает критичные драйверы дисков, монтирует реальный корень (Root FS /) и запускает первый пользовательский процесс init (PID 1, например systemd).
4. **Systemd (PID 1)**: Монтирует остальные ФС (/etc/fstab), запускает сервисы согласно target (default.target -> multi-user.target) и поднимает сети и консоли входа.`,
    codeSnippet: {
      language: 'bash',
      code: `systemd-analyze # посмотреть время загрузки компонентов ядра и сервисов
systemd-analyze blame # топ медленных сервисов при старте`
    },
    interviewTips: [
      'Утилита systemd-analyze blame производит впечатление на интервьюеров при обсуждении загрузки.'
    ],
    commonPitfalls: [
      'Забывать роль initramfs (временный RAM-диск с драйверами для монтирования главного диска).'
    ],
    tags: ['Linux', 'BootProcess', 'GRUB', 'Kernel', 'Systemd']
  },
  {
    id: 'linux-11',
    title: 'Что такое Swap и параметр vm.swappiness? Нужно ли отключать Swap для K8s?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Swap — файл/раздел на диске для подкачки неиспользуемых страниц памяти. vm.swappiness (0-100) задает агрессивность сброса. Для K8s традиционно Swap отключали для предсказуемости ресурсов.',
    fullAnswer: `**vm.swappiness**:
Значение от 0 до 100.
- 100: Ядро активно сбрасывает неиспользуемые анонимные страницы в Swap, освобождая Page Cache.
- 10: Ядро избегает использования Swap, пока есть свободная память.
- 0: Отключает сброс страниц в swap до наступления полного исчерпания RAM.

**Swap и Kubernetes**:
Kubelet исторически требовал отключенного swap (swapoff -a), так как алгоритмы планера (Scheduler) рассчитывают лимиты подов строго из физической RAM, и сброс страниц в медленный SSD/HDD приводит к срыву латентности (Throttling/Thrashing).
Начиная с K8s 1.28 появилась альфа-поддержка Swap с ограниченными лимитами, но по умолчанию его выключают.`,
    codeSnippet: {
      language: 'bash',
      code: `sudo swapoff -a # выключить swap мгновенно
sysctl vm.swappiness=10 # установить значение swappiness`
    },
    interviewTips: [
      'Объясните термин Memory Thrashing — когда ядро постоянно читает и пишет swap, парализуя диск.'
    ],
    commonPitfalls: [
      'Забывать закомментировать строку swap в /etc/fstab, из-за чего после перезагрузки сервера swap снова включается.'
    ],
    tags: ['Linux', 'Memory', 'Swap', 'Swappiness', 'Kubernetes']
  },
  {
    id: 'linux-12',
    title: 'Как работают межпроцессное взаимодействие (IPC) и сигналы в Linux?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'IPC — механизмы общения процессов: Signals, Pipes, Shared Memory, Message Queues, UNIX Sockets. Сигналы — асинхронные уведомления процесса ядра.',
    fullAnswer: `1. **Сигналы (Signals)**:
   - **SIGTERM (15)**: Просьба завершиться. Перехватывается процессом для Graceful Shutdown.
   - **SIGKILL (9)**: Принудительное уничтожение ядром. НЕЛЬЗЯ перехватить или проигнорировать!
   - **SIGHUP (1)**: Перечитка конфигурации приложения.
   - **SIGSEGV (11)**: Ошибка сегментации (обращение к чужой памяти).

2. **Способы IPC**:
   - **Pipes (Анонимные каналы)**: stdout | stdin.
   - **UNIX Domain Sockets**: Высокоскоростные сокеты файловой системы без затрат сетевого стека (например /var/run/docker.sock).
   - **Shared Memory (shm)**: Общий блок RAM для супер-быстрой передачи данных.`,
    codeSnippet: {
      language: 'bash',
      code: `kill -s SIGHUP 1234 # перечитать конфиг
ipcs -a # посмотреть общественные IPC ресурсы (Shared memory, semaphores)`
    },
    interviewTips: [
      'Укажите, что SIGKILL и SIGSTOP нельзя перехватить на уровне кода (Signal Handler).'
    ],
    commonPitfalls: [
      'Думать, что kill -9 всегда безопасен (может остановить процесс в середине транзакции БД).'
    ],
    tags: ['Linux', 'IPC', 'Signals', 'Kernel', 'Processes']
  },
  {
    id: 'linux-14',
    title: 'Как работают утилиты strace и tcpdump для отладки падающих приложений?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'strace перехватывает и записывает системные вызовы (syscalls) процесса к ядру. tcpdump захватывает сетевые пакеты на сетевом интерфейсе.',
    fullAnswer: `1. **strace**:
   - Показывает, какие файлы пытается открыть приложение (openat), куда пишет (write), сокеты (connect) и ошибки (ENOENT).
   - Идеально, когда приложение падает без логов на старте!
   - Пример: strace -f -e trace=file ./myapp — отследить попытки обращения к файловой системе.

2. **tcpdump**:
   - Использует libpcap для дампинга сетевого трафика на уровне L2-L4.
   - Пример: tcpdump -i eth0 port 53 -n — дампить только DNS запросы.
   - Записанный файл pcap можно открыть в Wireshark для визуального анализа.`,
    codeSnippet: {
      language: 'bash',
      code: `# Захват системных вызовов открытия файлов:
strace -f -e trace=open,openat ./app

# Захват HTTP трафика на порту 80:
sudo tcpdump -i any -n -X port 80`
    },
    interviewTips: [
      'Отметьте, что strace сильно замедляет работу процесса в продакшене из-за ptrace замедлений, использовать с осторожностью!'
    ],
    commonPitfalls: [
      'Запускать strace на нагруженной базе данных в продакшене без таймаутов.'
    ],
    tags: ['Linux', 'Debugging', 'Strace', 'Tcpdump', 'Troubleshooting']
  },
  {
    id: 'linux-28',
    title: 'Как Linux разрешает доменные имена в IP: разница между /etc/hosts, /etc/resolv.conf и nsswitch.conf?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Разрешение имен координируется файлом /etc/nsswitch.conf. Сначала система смотрит в статический файл /etc/hosts, затем опрашивает DNS-сервера, указанные в /etc/resolv.conf.',
    fullAnswer: `Алгоритм Name Resolution в Linux:
1. ** /etc/nsswitch.conf**: Задает приоритеты баз данных разрешения имен. В строке \`hosts: files dns\` указано, что локальные файлы приоритетнее удаленного DNS.
2. ** /etc/hosts**: Статический текстовый файл сопоставления IP и хостов. Применяется мгновенно для системных нужд или локальной разработки.
3. ** /etc/resolv.conf**: Содержит IP-адреса DNS-серверов (директива \`nameserver\`) и параметры поиска суффиксов доменов (\`search\`).

*Специфика systemd*: В современных дистрибутивах за DNS отвечает служба \`systemd-resolved\`. Она создает локальную петлю на IP \`127.0.0.53\` в resolv.conf, а реальные DNS настраиваются динамически в netplan/NetworkManager.`,
    codeSnippet: {
      language: 'bash',
      code: `# Просмотреть настройки приоритетов разрешения имен
cat /etc/nsswitch.conf | grep hosts

# Посмотреть текущие DNS-сервера от systemd-resolved
resolvectl status`
    },
    interviewTips: [
      'Если вас спросят про проблемы с таймаутами при подключении к БД по доменному имени, проверьте количество DNS серверов и опцию "options timeout:1 attempts:1" в resolv.conf.'
    ],
    commonPitfalls: [
      'Вручную редактировать /etc/resolv.conf на серверах с systemd-resolved — после перезагрузки или перезапуска сети файл перезапишется автогенерацией.'
    ],
    tags: ['Linux', 'Networking', 'DNS', 'Systemd']
  },
  {
    id: 'linux-29',
    title: 'Как узнать, какой процесс слушает определенный TCP/UDP порт (ss, netstat, lsof)?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Для выявления сокетов используются команды ss (современная, быстрая) или netstat (классическая, медленная), а также lsof -i для поиска открытых портов как файлов.',
    fullAnswer: `1. **ss (socket statistics)**:
   - Извлекает информацию напрямую из структур ядра (netlink), работает гораздо быстрее, чем netstat.
   - Популярные ключи: \`-tlnp\` (tcp, listening, numeric, show process).

2. **netstat**:
   - Устаревшая утилита (из пакета net-tools), которая читает /proc/net/tcp, что создает задержку при миллионах сокетов.

3. **lsof (list open files)**:
   - В Linux сокет — это тоже файл. Команда \`lsof -i :<PORT>\` мгновенно покажет процесс, PID и сокет.`,
    codeSnippet: {
      language: 'bash',
      code: `# Найти процесс на порту 80 через ss
sudo ss -tlnp | grep :80

# Поиск через lsof
sudo lsof -i :80`
    },
    interviewTips: [
      'Объясните значение букв ключа -tlnp: t — TCP, l — listening, n — numeric (без перевода IP/портов в имена хостов/сервисов для экономии времени), p — показать PID/имя процесса.'
    ],
    commonPitfalls: [
      'Запустить ss/netstat без root-прав (sudo). Команда покажет сокеты, но пустую колонку с именами процессов и PID.'
    ],
    tags: ['Linux', 'Networking', 'ss', 'lsof', 'Ports']
  },
  {
    id: 'linux-30',
    title: 'Как посмотреть таблицу маршрутизации в Linux и добавить статический маршрут?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Утилита ip route из пакета iproute2 — стандарт управления сетевыми маршрутами. Старые утилиты route -n и netstat -r не рекомендуются.',
    fullAnswer: `Сетевые маршруты определяют, через какой интерфейс и сетевой шлюз (Gateway) направлять пакеты.

Основные команды:
- \`ip route show\` — отобразить текущую таблицу маршрутизации.
- \`ip route add 192.168.100.0/24 via 10.0.0.1\` — направить весь трафик подсети 192.168.100.x через шлюз 10.0.0.1.
- \`ip route add default via 192.168.0.1\` — добавить default gateway (дефолтный маршрут для всего остального интернета).
- \`ip route del 192.168.100.0/24\` — удалить маршрут.`,
    codeSnippet: {
      language: 'bash',
      code: `# Показать таблицу маршрутов:
ip route

# Проверить, какой именно маршрут выберет ядро для адреса 8.8.8.8
ip route get 8.8.8.8`
    },
    interviewTips: [
      'Отметьте, что команды ip route меняют маршруты мгновенно в оперативной памяти ядра, но сбрасываются после перезапуска. Для персистентности нужно конфигурировать Netplan (на Ubuntu) или network-scripts (на CentOS).'
    ],
    commonPitfalls: [
      'Добавление конфликтующих default шлюзов, что ломает доступ к серверу по SSH.'
    ],
    tags: ['Linux', 'Networking', 'Routing', 'iproute2']
  },
  {
    id: 'linux-31',
    title: 'Как устроен доступ по SSH: структура ключей, authorized_keys, known_hosts и безопасность?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'SSH работает по асимметричному шифрованию. Приватный ключ хранится у клиента, публичный (.pub) добавляется на сервер в ~/.ssh/authorized_keys. База known_hosts предотвращает атаки MITM.',
    fullAnswer: `1. **authorized_keys**: Хранится на стороне сервера в каталоге пользователя. Содержит список доверенных публичных ключей. Если права на этот файл или папку ~/.ssh слишком открыты (например, chmod 777), SSH-демон из соображений безопасности заблокирует вход. Права должны быть: .ssh — 700, authorized_keys — 600.
2. **known_hosts**: Хранится на стороне клиента. Содержит слепки публичных хост-ключей серверов. При первом входе запрашивается подтверждение. При повторном входе, если ключ сервера изменился (например, сервер переустановили), клиент выдаст грозное предупреждение о возможной подмене хоста.

**Лучшие практики безопасности sshd_config:**
- Запретить авторизацию по паролям (PasswordAuthentication no).
- Запретить вход под root напрямую (PermitRootLogin no).
- Сменить стандартный порт 22 на нестандартный для защиты от брутфорс-ботов.`,
    codeSnippet: {
      language: 'bash',
      code: `# Настройка правильных прав доступа на стороне сервера:
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Быстрое копирование ключа с клиента на сервер
ssh-copy-id -i ~/.ssh/id_rsa.pub deployer@10.0.0.5`
    },
    interviewTips: [
      'Объясните роль ssh-agent: это демон на клиенте, который безопасно кеширует в оперативной памяти расшифрованные приватные ключи, избавляя от постоянного ввода пароля к ключу.'
    ],
    commonPitfalls: [
      'Выставлять права на ~/.ssh шире, чем положено. sshd откажет во входе без явного логирования ошибки на клиенте.'
    ],
    tags: ['Linux', 'SSH', 'Security', 'Cryptography']
  },
  {
    id: 'linux-32',
    title: 'Как запустить процесс в фоновом режиме и управлять фоновыми задачами (bg, fg, nohup, tmux)?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Добавление & запускает процесс в фоне текущего шелла. nohup защищает процесс от сигнала SIGHUP при закрытии терминала. screen/tmux — терминальные мультиплексоры для персистентных сессий.',
    fullAnswer: `При закрытии терминала SSH-сессия шлет сигнал SIGHUP (Hangup) всем запущенным дочерним процессам, завершая их работу.

1. **Фоновые утилиты шелла**:
   - \`command &\` — запуск в фоне. Процесс привязан к текущему терминалу.
   - \`jobs\` — посмотреть список задач текущей сессии.
   - \`fg %1\` — вернуть задачу №1 на передний план.
   - \`bg %1\` — возобновить приостановленную задачу в фоне.

2. **Защита от SIGHUP**:
   - \`nohup my_script.sh &> output.log &\` — процесс игнорирует сигнал зависания терминала и продолжает работать в фоне ОС.

3. **Мультиплексоры (tmux/screen)**:
   - Создают виртуальную сессию на сервере, изолированную от вашего сетевого подключения.
   - Если интернет оборвется, сессия со всеми окнами и запущенными компиляциями/скриптами продолжит жить в фоне. Вы сможете повторно подключиться через \`tmux attach\`.`,
    codeSnippet: {
      language: 'bash',
      code: `# Запуск долгой операции без риска прерывания
nohup tar -czf backup.tar.gz /data &> backup.log &

# Создание и подключение к сессии tmux
tmux new -s deploy-session
# Отключение от сессии: Ctrl+B, затем D
# Подключение обратно:
tmux attach -t deploy-session`
    },
    interviewTips: [
      'Использование tmux в повседневной работе системного инженера — это золотой стандарт. Упомяните, что все долгоиграющие миграции БД запускаются строго внутри tmux.'
    ],
    commonPitfalls: [
      'Запустить долгую сборку проекта по SSH напрямую в сессии, а затем потерять интернет-соединение, что убьет сборку на 99%.'
    ],
    tags: ['Linux', 'Tmux', 'Shell', 'Processes']
  },
  {
    id: 'linux-33',
    title: 'Основные файловые системы в Linux: в чем различие Ext4, XFS и Btrfs?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Ext4 — стабильная, универсальная файловая система по умолчанию с фиксированными инодами. XFS — ориентирована на огромные файлы, имеет динамическое выделение инод. Btrfs — ФС на базе Copy-on-Write с поддержкой снепшотов.',
    fullAnswer: `1. **Ext4**:
   - Самый популярный выбор. Стабильная, проверенная временем ФС с поддержкой журналирования.
   - Минус: Количество инод жестко фиксируется в момент форматирования тома.

2. **XFS**:
   - Высокопроизводительная 64-битная журналируемая ФС. Отлично масштабируется на дисковые массивы сверхбольших размеров.
   - Эффективно работает с большими файлами и многопоточным вводом-выводом. Иноды выделяются динамически. По умолчанию используется в RHEL/CentOS.

3. **Btrfs (B-Tree FS)**:
   - Файловая система нового поколения на базе архитектуры Copy-on-Write (CoW).
   - Поддерживает объединение дисков в RAID на уровне ФС, создание мгновенных снимков (Snapshots) без потери производительности, сжатие данных "на лету" (LZO, ZSTD) и дедупликацию.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть тип ФС всех смонтированных дисков
df -Th

# Форматирование раздела под XFS
sudo mkfs.xfs /dev/sdb1`
    },
    interviewTips: [
      'Для нагруженных СУБД (PostgreSQL) часто рекомендуют XFS из-за стабильной производительности при случайной перезаписи файлов.'
    ],
    commonPitfalls: [
      'Использовать Btrfs для баз данных с интенсивной записью без отключения атрибута CoW (флаг +C) — это приведет к ужасной фрагментации файлов БД.'
    ],
    tags: ['Linux', 'Filesystem', 'Ext4', 'XFS', 'Btrfs']
  },
  {
    id: 'linux-34',
    title: 'Как настроить сбор системных логов и работать с логированием через systemd-journald (journalctl)?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'systemd-journald собирает логи в бинарном формате. Утилита journalctl служит для парсинга, фильтрации и вывода этих логов по сервисам, времени и приоритетам.',
    fullAnswer: `Традиционный rsyslog пишет текстовые файлы в /var/log/. В противовес ему, systemd-journald хранит структурированные логи (содержит PID, UID, системные метаданные) в бинарном формате в памяти (/run/log/journal) или на диске (/var/log/journal).

**Фильтрация в journalctl**:
- \`journalctl -u nginx.service\` — логи конкретного юнита.
- \`journalctl -f\` — режим стриминга логов (аналог tail -f).
- \`journalctl -p err\` — фильтрация по уровню ошибок (priority: emerg, alert, crit, err, warning, notice, info, debug).
- \`journalctl --since "1 hour ago"\` — временной интервал.
- \`journalctl -k\` — только сообщения ядра (dmesg).`,
    codeSnippet: {
      language: 'bash',
      code: `# Просмотр ошибок конкретного сервиса за сегодня
journalctl -u docker.service --since today -p err

# Ограничить максимальный размер логов journald на диске
# Отредактировать /etc/systemd/journald.conf:
# SystemMaxUse=2G
sudo systemctl restart systemd-journald`
    },
    interviewTips: [
      'Упомяните, что бинарный формат логов journald защищает логи от подделки злоумышленниками в отличие от простых текстовых файлов.'
    ],
    commonPitfalls: [
      'Не ограничивать размер директории journald на диске, из-за чего логи могут съесть все свободное место в корневом разделе.'
    ],
    tags: ['Linux', 'Logging', 'Journalctl', 'Systemd']
  },
  {
    id: 'linux-35',
    title: 'Сетевая диагностика на уровнях L3-L4: в чем польза traceroute, mtr и dig?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'traceroute показывает цепочку роутеров до хоста. mtr объединяет функционал traceroute и ping в интерактивном режиме. dig опрашивает DNS сервера для проверки записей доменов.',
    fullAnswer: `1. **traceroute**:
   - Посылает пакеты с последовательно увеличивающимся TTL (Time To Live), начиная с 1. Каждая нода на пути откидывает пакет по истечении TTL и шлет назад ICMP Time Exceeded. Это позволяет построить путь пакета.
   - По умолчанию шлет UDP пакеты, но умеет ICMP (-I) и TCP (-T, полезно для обхода фаерволов).

2. **mtr (My Traceroute)**:
   - Постоянно пингует все промежуточные узлы. Наглядно показывает, на каком конкретно транзитном провайдере начинаются потери пакетов (Loss %) или задержки.

3. **dig (domain information groper)**:
   - Лучшая утилита для отладки DNS. Возвращает сырые ответы от DNS-серверов.
   - Позволяет запрашивать конкретные записи (A, MX, CNAME, TXT, NS, SOA) и опрашивать конкретные сервера напрямую (например @8.8.8.8).`,
    codeSnippet: {
      language: 'bash',
      code: `# Трассировка по TCP порту 443 (для обхода блокировок ICMP)
sudo traceroute -T -p 443 google.com

# Запрос TXT записи домена с конкретного DNS сервера
dig @1.1.1.1 github.com TXT`
    },
    interviewTips: [
      'Умение читать вывод dig (особенно флаги в заголовке, секцию ANSWER SECTION и поле TTL) — обязательный навык для решения сетевых задач.'
    ],
    commonPitfalls: [
      'Использовать nslookup вместо dig. nslookup устарел, имеет неконсистентное поведение на разных ОС и не показывает детальные флаги DNS-ответа.'
    ],
    tags: ['Linux', 'Networking', 'DNS', 'Troubleshooting']
  },
  {
    id: 'linux-36',
    title: 'Как настроить синхронизацию точного времени в Linux (NTP, chrony, timedatectl)?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Точное время синхронизируется по протоколу NTP. В современных дистрибутивах за это отвечают службы chronyd (более быстрая и адаптивная) или systemd-timesyncd.',
    fullAnswer: `Рассинхронизация времени на серверах ломает базы данных (репликацию, кластеры), аутентификацию (Kerberos, TOTP), ротацию токенов и сбивает таймстампы логов, затрудняя расследования инцидентов.

1. **chrony**:
   - Современная реализация NTP. Идеально подходит для виртуальных машин, которые часто засыпают/просыпаются.
   - Корректирует время плавно (slew) без резких скачков назад, которые могут повредить СУБД.

2. **systemd-timesyncd**:
   - Легковесный клиент, встроенный в systemd. Синхронизирует время по SNTP, не является полноценным NTP-сервером.

3. **timedatectl**:
   - Утилита для настройки часового пояса и включения сетевой синхронизации.`,
    codeSnippet: {
      language: 'bash',
      code: `# Проверить текущий статус времени и синхронизации
timedatectl status

# Посмотреть список NTP-серверов и статус синхронизации в chrony
chronyc sources -v`
    },
    interviewTips: [
      'Объясните разницу между "step" (резкая корректировка времени скачком) и "slew" (плавное ускорение или замедление системных часов на доли миллисекунд). СУБД ненавидят step-корректировки назад во времени.'
    ],
    commonPitfalls: [
      'Забывать включить NTP синхронизацию (timedatectl set-ntp true), из-за чего часы сервера постепенно уплывают.'
    ],
    tags: ['Linux', 'NTP', 'Chrony', 'Maintenance']
  },
  {
    id: 'linux-37',
    title: 'Что означают детальные состояния CPU в выводе top (%user, %system, %iowait, %idle, %steal)?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Показатели распределяют процессорное время ядра: us (пользовательский код), sy (код ядра/системные вызовы), wa (ожидание диска), id (бездействие), st (заимствование гипервизором).',
    fullAnswer: `Понимание метрик загрузки процессора позволяет быстро найти бутылочное горлышко:

- **%us (user)**: Время, затраченное на выполнение процессов пользователей в userspace (ваше приложение, nginx, компиляция).
- **%sy (system)**: Время работы ядра (kernelspace) по обработке прерываний, переключению контекста и выполнению системных вызовов (чтение диска, отправка пакетов). Высокий %sy говорит о плохой архитектуре софта или проблемах с драйверами.
- **%wa (iowait)**: Время ожидания процессором завершения операций ввода-вывода (I/O) с диском или сетью. Процессор простаивает, так как диск не успевает обрабатывать запросы.
- **%id (idle)**: Процент времени бездействия процессора. Чем больше, тем свободнее CPU.
- **%st (steal)**: Проявляется на виртуалках. Время, "украденное" у вашей ВМ гипервизором хоста для обслуживания других виртуальных машин. Высокий %st указывает на жесткий "оверселлинг" со стороны провайдера.`,
    codeSnippet: {
      language: 'bash',
      code: `# Быстрый вывод строки CPU из top без интерактивного входа
top -b -n 1 | grep '%Cpu'`
    },
    interviewTips: [
      'Свяжите %wa с I/O-bound процессами в состоянии D, а высокий %st — с плохим облачным провайдером или шумными соседями (Noisy Neighbors).'
    ],
    commonPitfalls: [
      'Думать, что если CPU load 100%, то виноват код приложения. При высоком %wa виновен медленный накопитель, а при %sy — избыток системных прерываний.'
    ],
    tags: ['Linux', 'CPU', 'Performance', 'Troubleshooting']
  },
  {
    id: 'linux-38',
    title: 'Расширенные права доступа (Access Control Lists - ACL): как использовать getfacl и setfacl?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'ACL расширяет традиционную модель прав (UGO), позволяя назначать индивидуальные права на чтение/запись/выполнение для конкретных пользователей или групп вне основных связок.',
    fullAnswer: `Традиционная модель Linux позволяет назначить права только одному Владельцу, одной Группе и Всем остальным. Если нужно дать права на запись второму конкретному пользователю, не добавляя его в группу первого, классический chmod бессилен. На помощь приходит ACL.

Основные утилиты:
- **getfacl <path>** — просмотреть текущие расширенные права файла/директории.
- **setfacl -m u:<username>:<perms> <path>** — добавить или изменить права доступа для конкретного пользователя.
- **setfacl -m g:<groupname>:<perms> <path>** — изменить права для конкретной группы.
- **setfacl -x u:<username> <path>** — удалить ACL правила для пользователя.
- **setfacl -d -m u:<username>:<perms> <directory>** — установить default права. Все новые файлы в папке автоматически получат эти права.`,
    codeSnippet: {
      language: 'bash',
      code: `# Назначить пользователю 'ivan' права на чтение и запись файла config.json
setfacl -m u:ivan:rw config.json

# Проверить установленные ACL (у файла в выводе ls появится символ '+' в правах)
getfacl config.json`
    },
    interviewTips: [
      'Упомяните, что для поддержки ACL на старых ядрах файловая система должна быть смонтирована с опцией "acl" в fstab (в современных ядрах Ext4/XFS поддерживает это из коробки).'
    ],
    commonPitfalls: [
      'Забывать, что утилиты бэкапа и копирования (кроме rsync с флагом -A и tar c флагом --acls) могут терять ACL-атрибуты файлов.'
    ],
    tags: ['Linux', 'Security', 'ACL', 'Permissions']
  },
  {
    id: 'linux-39',
    title: 'Как определить, что у приложения утекает оперативная память и как найти виновника?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Утечка памяти характеризуется монотонным ростом RSS процесса без его освобождения. Поиск производится утилитами ps, pmap, gdb, valgrind или профилировщиками.',
    fullAnswer: `Алгоритм траблшутинга утечки памяти:
1. **Мониторинг**: Заметить постепенное снижение доступной памяти (available) в системе и рост Swap.
2. **Поиск процесса**: Отсортировать процессы по потреблению физической памяти (RSS): \`ps aux --sort=-rss | head -n 10\`. Зафиксировать PID процесса.
3. **Анализ карты памяти**: Утилита \`pmap -x <PID>\` покажет детальную карту адресного пространства процесса. Если блоки anon (анонимная память) растут и не очищаются — это признак утечки.
4. **Профилирование**:
   - Для приложений на C/C++: запуск под \`valgrind --tool=memcheck\`.
   - Для интерпретируемых языков (Go, Python, Java): подключение встроенных профайлеров (pprof, tracemalloc, jmap) для снятия дампов кучи (Heap Dumps) и их сравнения во времени.`,
    codeSnippet: {
      language: 'bash',
      code: `# Топ-5 процессов по потреблению реальной памяти (RSS)
ps -eo pid,ppid,cmd,%mem,rss --sort=-rss | head -n 6

# Детальный просмотр аллокаций памяти процесса 1234
sudo pmap -x 1234 | tail -n 15`
    },
    interviewTips: [
      'Умение читать Heap Dumps и понимать разницу между физической памятью и виртуальной — маркер сильного инженера.'
    ],
    commonPitfalls: [
      'Спутать утечку памяти с нормальной работой кэша (Page Cache), который забивает всю свободную память, но мгновенно освобождается при запросе.'
    ],
    tags: ['Linux', 'Memory', 'Troubleshooting', 'Debugging']
  },
  {
    id: 'linux-40',
    title: 'Что такое процессы-зомби (Zombie) и сироты (Orphan) в Linux и как их очистить?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'Зомби — завершенный процесс, чей родитель не прочитал код возврата системным вызовом wait(). Сирота — активный процесс, чей родитель умер; его усыновляет процесс systemd (PID 1).',
    fullAnswer: `1. **Процессы-зомби (Zombie / Defunct, статус Z в ps)**:
   - Фактически они мертвы, не занимают процессорное время и оперативную память.
   - Они занимают только запись в таблице процессов ядра (PID). Если зомби станет слишком много, ОС не сможет генерировать новые PID.
   - **Как убить**: Команда \`kill -9\` не работает на зомби, так как процесс уже мертв. Нужно отправить сигнал \`SIGCHLD\` родительскому процессу, чтобы он сделал \`wait()\`, либо завершить самого родителя.

2. **Процессы-сироты (Orphan)**:
   - Продолжают нормально выполняться в фоне. При потере родителя ядро переназначает их родителем PID 1 (systemd/init).
   - Никакого вреда системе они не несут, если не ведут себя деструктивно. Регулируются стандартным способом.`,
    codeSnippet: {
      language: 'bash',
      code: `# Поиск зомби в системе:
ps aux | awk '{if ($8 == "Z" || $8 ~ /defunct/) print $0}'

# Найти PID родителя зомби (PPID), чтобы пнуть его сигналом SIGCHLD
ps -o pid,ppid,status,cmd | grep defunct`
    },
    interviewTips: [
      'Объясните, почему убить зомби напрямую нельзя. "Они уже мертвы, у них нет тела, только имя в свидетельстве о рождении ядра".'
    ],
    commonPitfalls: [
      'Бесконечно слать kill -9 на PID зомби-процесса и удивляться, почему он не исчезает.'
    ],
    tags: ['Linux', 'Processes', 'Zombie', 'Kernel']
  },
  {
    id: 'linux-41',
    title: 'Как ограничить системные ресурсы для пользователей и процессов с помощью ulimit и limits.conf?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'ulimit задает ограничения ресурсов оболочки (лимиты на файлы, память, процессы). Системные лимиты настраиваются в файле /etc/security/limits.conf.',
    fullAnswer: `Для защиты ОС от исчерпания ресурсов (Fork-бомбы, утечки памяти) используются лимиты ресурсов.

Существует два типа лимитов:
1. **Soft Limit (Мягкий лимит)**: Значение, которое пользователь может увеличить самостоятельно в пределах жесткого лимита.
2. **Hard Limit (Жесткий лимит)**: Физический потолок, устанавливаемый суперпользователем (root).

Основные параметры в \`/etc/security/limits.conf\`:
- **nofile**: Максимальное количество открытых файловых дескрипторов (важно для высоконагруженных Nginx/Elasticsearch).
- **nproc**: Максимальное число запущенных процессов/потоков для пользователя.
- **as**: Максимальный размер адресного пространства процесса.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть текущий лимит открытых файлов
ulimit -n

# Временное увеличение лимита в текущей сессии
ulimit -n 65535

# Конфигурация в /etc/security/limits.conf для пользователя deployer:
# deployer   soft   nofile   65535
# deployer   hard   nofile   131072`
    },
    interviewTips: [
      'При установке СУБД или брокеров сообщений всегда проверяйте ulimit. Значение по умолчанию (1024) очень быстро приведет к ошибке "Too many open files".'
    ],
    commonPitfalls: [
      'Забывать, что сервисы, запускаемые через systemd, игнорируют limits.conf. Для них лимиты настраиваются директивами LimitNOFILE= в файле юнита.'
    ],
    tags: ['Linux', 'Security', 'ulimit', 'Performance']
  },
  {
    id: 'linux-42',
    title: 'Что такое RAID массивы (0, 1, 5, 10) и как диагностировать состояние софтового RAID mdadm?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'RAID объединяет физические диски в один логический элемент для скорости или надежности. Управление софтовым RAID в Linux осуществляется утилитой mdadm.',
    fullAnswer: `Типы RAID:
- **RAID 0 (Striping)**: Данные делятся поочередно между дисками. Скорость x2, надежность падает (вылет одного диска уничтожает весь массив).
- **RAID 1 (Mirroring)**: Зеркалирование дисков. Высокая надежность (выживет 1 диск из 2), скорость чтения x2, объем равен емкости 1 диска.
- **RAID 5 (Parity)**: Данные и контрольные суммы чередуются по всем дискам. Выдерживает отказ 1 диска. Требует минимум 3 диска.
- **RAID 10 (1+0)**: Зеркалированный массив, данные которого чередуются. Высокая скорость и отказоустойчивость, требует минимум 4 диска.

В Linux программный RAID создается демоном md (Multiple Devices) и конфигурируется через \`mdadm\`.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть текущее состояние всех RAID массивов в системе
cat /proc/mdstat

# Вывести детальную информацию о массиве md0
sudo mdadm --detail /dev/md0`
    },
    interviewTips: [
      'Объясните статус "degraded" (деградировал) в выводе mdstat — это означает, что один или несколько дисков вышли из строя, массив работает без резервирования, и требуется замена диска и rebuild.'
    ],
    commonPitfalls: [
      'Забывать настроить алертинг на состояние /proc/mdstat, из-за чего о смерти первого диска в RAID 1 узнают только после падения второго диска.'
    ],
    tags: ['Linux', 'Hardware', 'RAID', 'mdadm', 'Troubleshooting']
  },

  // =========================================================================
  // ============================ SENIOR LEVEL ===============================
  // =========================================================================
  {
    id: 'linux-2',
    title: 'Что такое OOM Killer в Linux и как настроить oom_score_adj?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'OOM Killer (Out Of Memory Killer) — механизм ядра Linux, который при критической нехватке оперативной памяти принудительно завершает самый подходящий процесс для спасения системы.',
    fullAnswer: `Ядро выбирает жертву на основе очков oom_score (диапазон от 0 до 1000). Формула зависит от процента занимаемой оперативной памяти процессом и его дочерними процессами.
Мы можем повлиять на приоритет уничтожения процессом с помощью параметра oom_score_adj (диапазон от -1000 до +1000):
- -1000 — запрещает OOM Killer трогать данный процесс (используется для sshd, kubelet, критичных СУБД).
- +1000 — делает процесс первейшим кандидатом на уничтожение.

Значения хранятся в виртуальной файловой системе: /proc/<PID>/oom_score and /proc/<PID>/oom_score_adj.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть текущий балл OOM процесса nginx (PID 1234)
cat /proc/1234/oom_score

# Защитить важный сервис от OOM Killer
echo -900 > /proc/1234/oom_score_adj`
    },
    interviewTips: [
      'Свяжите с Kubernetes: K8s использует oom_score_adj для реализации Quality of Service (QoS) классов подов: Guaranteed (-997), Burstable (от 2 до 999) и BestEffort (1000).'
    ],
    commonPitfalls: [
      'Думать, что Swap полностью предотвратит OOM. На самом деле Swap лишь дает время, но при активном thrashing система сильно ляжет.'
    ],
    tags: ['Linux', 'Kernel', 'OOM', 'Memory', 'Kubernetes']
  },
  {
    id: 'linux-13',
    title: 'Что такое eBPF (Extended Berkeley Packet Filter) и почему это революция в Linux?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'eBPF позволяет безопасным образом запускать пользовательский байт-код прямо ВНУТРИ ядра Linux без пересборки ядра и без написания опасных Kernel Modules.',
    fullAnswer: `Традиционно для добавления функционала в ядро создавались модули ядра (KMOD). Ошибка в модуле приводит к Kernel Panic.

**Как работает eBPF**:
1. Программа на C/Go компилируется в eBPF байт-код.
2. При загрузке в ядро **eBPF Verifier** строго проверяет байт-код на отсутствие зацикливаний, утечек и обращений к чужой памяти.
3. Программа выполняется в JIT-компиляторе с производительностью нативного ядра!

**Сферы применения**:
- Сети и Security: Cilium CNI обработка пакетов в миную iptables (10x быстрее).
- Observability: BPFtrace, Cilium Hubble — перехват любых системных вызовов с нулевым накладным расходом.`,
    codeSnippet: {
      language: 'bash',
      code: `bpftrace -e 'tracepoint:syscalls:sys_enter_open { printf("%s %s\\n", comm, str(args->filename)); }'`
    },
    interviewTips: [
      'Назовите Cilium CNI как главный успех eBPF в Kubernetes экосистеме.'
    ],
    commonPitfalls: [
      'Путать eBPF с классическим фаерволом iptables.'
    ],
    tags: ['Linux', 'eBPF', 'Kernel', 'Cilium', 'Observability']
  },
  {
    id: 'linux-43',
    title: 'Что такое Linux Kernel Namespaces (пространства имен) и как они реализуют изоляцию контейнеров?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Namespaces — технология ядра Linux, обеспечивающая изоляцию глобальных системных ресурсов между группами процессов. Это фундамент контейнеризации (Docker, LXC).',
    fullAnswer: `В отличие от аппаратной виртуализации (KVM, ESXi), контейнеры используют общее ядро хоста. Изоляцию обеспечивают пространства имен (Namespaces):

1. **PID Namespace**: Изолирует дерево процессов. Процесс в контейнере видит себя как PID 1, но на хосте имеет обычный высокий PID (например 24152).
2. **NET Namespace**: Изолирует сетевые устройства, порты, таблицы маршрутизации. У каждого контейнера свой loopback, интерфейсы (veth-пары) и порты.
3. **MNT (Mount) Namespace**: Изолирует точки монтирования файловой системы. Процесс видит только свою файловую иерархию (chroot на стероидах).
4. **IPC Namespace**: Изолирует ресурсы межпроцессного взаимодействия (очереди сообщений, семафоры, разделяемую память).
5. **UTS Namespace**: Позволяет иметь индивидуальные hostname и имя домена для каждого контейнера.
6. **USER Namespace**: Изолирует UID/GID. Пользователь root (UID 0) внутри контейнера мапится на непривилегированного пользователя (например, UID 10001) на хосте.`,
    codeSnippet: {
      language: 'bash',
      code: `# Запуск bash в изолированном пространстве имен сети и процессов
sudo unshare --fork --pid --net --mount-proc bash

# Посмотреть пространства имен запущенных процессов в системе
lsns -t net`
    },
    interviewTips: [
      'Объясните, почему Docker-контейнер — это не виртуалка, а обычный процесс на хосте, изолированный с помощью namespaces и ограниченный cgroups.'
    ],
    commonPitfalls: [
      'Считать, что namespaces обеспечивают 100% изоляцию. Ядро общее, поэтому уязвимость в ядре (Kernel Exploit) позволяет легко совершить побег из контейнера (Container Escape).'
    ],
    tags: ['Linux', 'Kernel', 'Namespaces', 'Docker', 'Containers']
  },
  {
    id: 'linux-44',
    title: 'Что такое Control Groups (cgroups v1 и v2) и как они ограничивают ресурсы (CPU, RAM)?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Control Groups (cgroups) — механизм ядра Linux, который ограничивает, изолирует и измеряет потребление ресурсов (процессор, память, дисковый ввод-вывод) для групп процессов.',
    fullAnswer: `Если пространства имен (namespaces) отвечают за то, что процесс *может видеть*, то cgroups отвечают за то, сколько ресурсов процесс *может потребить*.

**Ключевые контроллеры cgroups:**
- **memory**: Задает лимиты ОЗУ. При превышении жесткого лимита (limit_in_bytes) ядро шлет процессу OOM.
- **cpu**: Задает доли процессорного времени (shares) или жесткие квоты (cfs_quota_us / cfs_period_us).
- **blkio**: Ограничивает скорость чтения/записи на диски (IOPS, bps).

**Разница v1 и v2**:
- **cgroups v1**: Каждый ресурс (CPU, RAM, BlkIO) управлялся отдельной, независимой иерархией каталогов. Приводило к рассинхронизации и сложной архитектуре.
- **cgroups v2**: Единая унифицированная иерархия процессов. Один процесс не может находиться в разных группах для разных ресурсов. Кардинально улучшено распределение I/O и работы с OOM. Поддерживается во всех современных K8s дистрибутивах.`,
    codeSnippet: {
      language: 'bash',
      code: `# Просмотр смонтированных cgroups v2 контроллеров
mount | grep cgroup2

# Путь к cgroup лимитам процесса в системе:
cat /sys/fs/cgroup/user.slice/user-1000.slice/memory.max`
    },
    interviewTips: [
      'Свяжите cgroups с лимитами ресурсов в Kubernetes (resources.limits.cpu/memory). Объясните, что CPU-лимиты реализуются через CFS (Completely Fair Scheduler) Throttling, а лимиты памяти — через OOM Killer.'
    ],
    commonPitfalls: [
      'Думать, что CPU limit в контейнере работает как замедление частоты процессора. На самом деле процесс просто приостанавливается (throttled) на долю миллисекунд в каждом цикле планировщика.'
    ],
    tags: ['Linux', 'Kernel', 'cgroups', 'Performance', 'Docker']
  },
  {
    id: 'linux-45',
    title: 'Как ядро Linux сбрасывает "грязные" страницы памяти на диск: vm.dirty_background_ratio и vm.dirty_ratio?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Параметры sysctl регулируют поведение Page Cache при записи на диск. background_ratio запускает фоновый сброс ядром, а ratio блокирует процессы для принудительной записи.',
    fullAnswer: `Когда процесс пишет данные на диск (системный вызов \`write\`), ядро не отправляет их на физический накопитель мгновенно. Данные буферизируются в RAM (Page Cache), и страница памяти помечается как "грязная" (Dirty Page).

Параметры оптимизации дисковой записи:
1. **vm.dirty_background_ratio** (по умолчанию ~10%):
   - Процент от общей оперативной памяти. При превышении этого порога "грязными" страницами ядро в фоновом режиме запускает поток \`pdflush\` / \`kworker\` для асинхронного сброса данных на диск. Процессы продолжают писать в RAM без блокировок.
2. **vm.dirty_ratio** (по умолчанию ~20%):
   - Критический порог. Если объем грязных страниц превышает этот процент, ядро блокирует все новые системные вызовы \`write\` от приложений и заставляет их принудительно сбрасывать свои буферы на диск синхронно. Возникает жесткий фриз (I/O Spike).

*Для высоконагруженных систем* (СУБД на медленных дисках) эти значения занижают (например, background_ratio=5, ratio=10), чтобы сброс происходил мелкими порциями постоянно, избегая огромных лагов при забивании буфера.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть текущий объем "грязных" страниц в RAM
cat /proc/meminfo | grep -i dirty

# Настройка агрессивного фонового сброса в sysctl
sudo sysctl -w vm.dirty_background_ratio=5
sudo sysctl -w vm.dirty_ratio=10`
    },
    interviewTips: [
      'Объясните опасность больших значений dirty_ratio при внезапном отключении питания: все незаписанные Dirty-страницы из RAM будут безвозвратно утеряны.'
    ],
    commonPitfalls: [
      'Оставлять дефолтные значения (например dirty_ratio=40 на сервере со 128ГБ RAM), что приведет к застреванию 50ГБ данных в памяти и парализующему фризу при сбросе.'
    ],
    tags: ['Linux', 'Memory', 'Kernel', 'I/O', 'Performance']
  },
  {
    id: 'linux-46',
    title: 'Что такое Overcommit Memory (vm.overcommit_memory) и почему это критично для СУБД (Redis, PostgreSQL)?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Overcommit Memory — стратегия ядра выделять процессам больше виртуальной памяти, чем физически есть в RAM/Swap. Поведение регулируется параметром vm.overcommit_memory (0, 1, 2).',
    fullAnswer: `Процессы часто запрашивают память "с запасом", но реально используют лишь ее часть. Ядро Linux пользуется этим и одобряет избыточные запросы на выделение памяти (Overcommit).

Параметр **vm.overcommit_memory** принимает три значения:
- **0** (Heuristic, по умолчанию): Ядро оценивает размер запроса эвристически. Очевидный перебор блокирует, небольшие превышения разрешает.
- **1** (Always): Ядро всегда одобряет любые запросы на выделение памяти. Критично для **Redis**, который использует системный вызов \`fork()\` для создания фонового процесса сохранения базы (BGSAVE) через механизм Copy-on-Write. Если overcommit=0, fork может упасть с ошибкой нехватки памяти.
- **2** (Never): Ядро запрещает перерасход. Общий объем выделенной памяти ограничен формулой: \`Swap + (RAM * vm.overcommit_ratio / 100)\`. Предотвращает непредсказуемый OOM, гарантируя стабильность для серверов баз данных вроде PostgreSQL.`,
    codeSnippet: {
      language: 'bash',
      code: `# Рекомендуемая настройка для Redis
sudo sysctl -w vm.overcommit_memory=1

# Проверить текущий лимит выделения при overcommit=2
cat /proc/meminfo | grep -i commit`
    },
    interviewTips: [
      'Упомяните, что Redis в логах прямо просит выставить vm.overcommit_memory=1 для предотвращения сбоев BGSAVE. Это отличный пример связки ОС и прикладного ПО.'
    ],
    commonPitfalls: [
      'Установить overcommit_memory=2 на сервере без Swap, что вызовет отказы выделения памяти приложениям при малейшем пике, даже если физически свободная RAM еще осталась.'
    ],
    tags: ['Linux', 'Memory', 'Kernel', 'Redis', 'PostgreSQL']
  },
  {
    id: 'linux-47',
    title: 'Оптимизация сетевого стека Linux под высокие нагрузки (somaxconn, tcp_max_syn_backlog)?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Для обработки миллионов сетевых соединений нужно расширять буферы сокетов ядра. Ключевые параметры: net.core.somaxconn (очередь готовых соединений) и net.ipv4.tcp_max_syn_backlog (очередь полуоткрытых соединений).',
    fullAnswer: `При установлении TCP соединения (3-way handshake) пакеты проходят через системные очереди ядра:

1. **net.ipv4.tcp_max_syn_backlog**:
   - Размер очереди полуоткрытых соединений (клиент прислал SYN, сервер ответил SYN-ACK, но еще не получил ACK). При забивании этой очереди сервер начинает дропать новые входящие пакеты SYN (клиенты получают Connection Timeout).
   - Защита от SYN Flood атак также задействует параметр \`net.ipv4.tcp_syncookies\`.

2. **net.core.somaxconn**:
   - Максимальный размер очереди полностью установленных TCP-соединений, ожидающих вызова \`accept()\` со стороны веб-сервера (Nginx, Gunicorn). Значение по умолчанию (обычно 128) крайне мало для продакшена.
   - Также необходимо увеличивать параметр \`backlog\` в конфиге самого Nginx, иначе системное увеличение не даст эффекта.

3. **net.ipv4.tcp_tw_reuse**:
   - Позволяет повторно использовать сокеты, находящиеся в состоянии TIME_WAIT, для новых исходящих соединений (актуально при нехватке портов).`,
    codeSnippet: {
      language: 'bash',
      code: `# Оптимальные настройки sysctl для нагруженного веб-сервера (/etc/sysctl.conf)
net.core.somaxconn = 65535
net.ipv4.tcp_max_syn_backlog = 65535
net.ipv4.tcp_tw_reuse = 1
net.ipv4.ip_local_port_range = 1024 65535

# Применить изменения мгновенно
sudo sysctl -p`
    },
    interviewTips: [
      'Свяжите параметры somaxconn с системными метриками SYN FLOOD и ошибками "Connection reset by peer" в логах веб-серверов.'
    ],
    commonPitfalls: [
      'Увеличить somaxconn в sysctl до 65535, но оставить listen-порт в Nginx со стандартным значением backlog (по умолчанию 511). Очередь будет урезана по наименьшему значению.'
    ],
    tags: ['Linux', 'Networking', 'Sysctl', 'Performance', 'Nginx']
  },
  {
    id: 'linux-48',
    title: 'Как работают I/O шедулеры в Linux (mq-deadline, bfq, kyber, none) и какой выбрать для SSD/NVMe?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'I/O шедулер оптимизирует очередность выполнения операций чтения/записи на диск. Для современных SSD и NVMe накопителей выбирают шедулеры none (none/noop) или kyber.',
    fullAnswer: `Исторически диски были вращающимися (HDD), где физическая головка должна была перемещаться по пластинам. Планировщики (например CFQ, Deadline) сортировали запросы по секторам диска (алгоритм лифта), чтобы минимизировать перемещение головки.

С появлением твердотельных накопителей (SSD) и шины NVMe концепция изменилась — время случайного доступа минимально, а диски поддерживают параллельную обработку тысяч очередей на аппаратном уровне (Multi-Queue Block I/O, blk-mq).

Современные планировщики:
1. **none / noop**:
   - Полное отсутствие планирования на уровне ОС. Запросы передаются напрямую контроллеру накопителя, который сам распределяет их на аппаратном уровне.
   - **Рекомендуется**: Для сверхбыстрых NVMe дисков и виртуальных машин (где планированием занимается хост-гипервизор).
2. **kyber**:
   - Разработан Facebook для быстрых устройств. Нацелен на минимизацию задержек (Latency). Измеряет время ответа и динамически регулирует глубину очередей.
   - **Рекомендуется**: Для производительных SATA/NVMe SSD под смешанной нагрузкой.
3. **mq-deadline**:
   - Версия классического deadline для мультиочередей. Дает приоритет чтению перед записью для предотвращения затыков приложений.
   - **Рекомендуется**: Для обычных SATA SSD и HDD.
4. **bfq (Budget Fair Queueing)**:
   - Гарантирует честное распределение полосы пропускания между процессами, предотвращая монополизацию диска одним тяжелым копированием. Имеет высокий CPU-оверхед.
   - **Рекомендуется**: Для десктопов и HDD-накопителей.`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть текущий и доступные планировщики для диска nvme0n1
cat /sys/block/nvme0n1/queue/scheduler

# Изменить планировщик диска на "none" на лету
echo "none" | sudo tee /sys/block/nvme0n1/queue/scheduler`
    },
    interviewTips: [
      'Назовите переход на архитектуру blk-mq (Multi-Queue) в современных ядрах Linux ключевым фактором отказа от старых планировщиков типа cfq.'
    ],
    commonPitfalls: [
      'Использовать bfq на облачных серверах с быстрыми SSD, тратя драгоценные такты процессора на сложный расчет очередей на стороне ОС.'
    ],
    tags: ['Linux', 'Hardware', 'Disk', 'Kernel', 'I/O']
  },
  {
    id: 'linux-49',
    title: 'Как устроены блокировки файлов в Linux: разница между Advisory и Mandatory locking (flock, fcntl)?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'Advisory блокировки (рекомендательные) требуют, чтобы процессы сами проверяли наличие блокировок. Mandatory (обязательные) форсируются ядром на уровне файловой системы.',
    fullAnswer: `Блокировки предотвращают одновременную перезапись файла несколькими процессами (Race Conditions).

1. **Advisory Locking (Рекомендательные блокировки)**:
   - Самый частый тип. Процессы ведут себя "вежливо": перед записью файла вызывают \`flock()\` или \`fcntl()\`, запрашивая лок.
   - Если другой процесс проигнорирует блокировку и сделает прямой системный вызов \`write()\` без проверки лока, ядро разрешит ему запись и перезапишет данные.
   - Реализуется через:
     - \`flock()\`: Простая блокировка всего файла целиком. Привязана к файловому дескриптору.
     - \`fcntl()\`: Сложная побайтовая блокировка (Record Locking). Позволяет заблокировать отдельный диапазон байт внутри файла.

2. **Mandatory Locking (Обязательные блокировки)**:
   - Блокировка жестко форсируется ядром. Если на файл наложен mandatory lock, то любой сторонний вызов \`read()\` или \`write()\` от любого процесса будет заблокирован ядром.
   - Чтобы включить обязательные блокировки, файловая система должна быть смонтирована с флагом mand, а на самом файле должны быть сброшены права на выполнение группой при активном SGID бите.
   - *Нюанс*: Начиная с ядер Linux 5.15 поддержка mandatory locking полностью удалена из соображений безопасности и нестабильности (любой юзер мог легко вызвать отказ в обслуживании системы).`,
    codeSnippet: {
      language: 'bash',
      code: `# Наложить рекомендательную блокировку на файл в bash скрипте
exec 3> /var/run/mytest.lock
flock -x 3 # заблокировать дескриптор 3 в эксклюзивном режиме
echo "Безопасная запись" >&3
flock -u 3 # разблокировать`
    },
    interviewTips: [
      'Объясните применение блокировок файлов при запуске cron-скриптов: утилита flock гарантирует, что если предыдущий скрипт еще не отработал, новый инстанс скрипта завершится, не создавая дублей нагрузки.'
    ],
    commonPitfalls: [
      'Надеяться на mandatory locking в современном Linux, не зная, что поддержка этой технологии удалена из ядра.'
    ],
    tags: ['Linux', 'Filesystem', 'Locking', 'Concurrency']
  },
  {
    id: 'linux-50',
    title: 'Архитектурные отличия системных вызовов select, poll и epoll для обработки сетевых соединений?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'select и poll сканируют весь список сокетов линейно O(N), что неэффективно при росте соединений. epoll использует событийный подход O(1), уведомляя только об активных сокетах.',
    fullAnswer: `Для мультиплексирования ввода-вывода высоконагруженные сервера (Nginx, Redis) должны отслеживать тысячи сетевых сокетов одновременно.

1. **select()**:
   - Исторически первый системный вызов.
   - Принимает битовые маски дескрипторов файлов. Ограничен жестким лимитом в 1024 дескриптора (FD_SETSIZE).
   - При каждом вызове копирует список дескрипторов из userspace в kernelspace. При готовности данных возвращает весь список — приложению приходится линейно обходить все сокеты O(N), чтобы найти готовый.

2. **poll()**:
   - Избавлен от лимита в 1024 дескриптора (использует массив структур).
   - В остальном имеет те же минусы: копирование массивов туда-обратно и линейный обход O(N).

3. **epoll()** (Linux-specific):
   - Создает структуру в ядре (красно-черное дерево для отслеживания дескрипторов и двусвязный список для готовых событий).
   - Работает по событийной схеме (event-driven). Когда на сетевую карту приходит пакет, сетевая карта генерирует прерывание, ядро обрабатывает его и помещает готовый дескриптор в список готовых событий.
   - Системный вызов возвращает ТОЛЬКО активные сокеты. Сложность работы всегда составляет O(1), независимо от количества неактивных соединений (хоть миллион).`,
    codeSnippet: {
      language: 'c',
      code: `/* Псевдокод использования epoll_wait */
int epoll_fd = epoll_create1(0);
struct epoll_event event;
event.events = EPOLLIN; // отслеживать только чтение
event.data.fd = server_socket;
epoll_ctl(epoll_fd, EPOLL_CTL_ADD, server_socket, &event);

// Событийный цикл O(1)
int num_events = epoll_wait(epoll_fd, events_buffer, MAX_EVENTS, -1);
for (int i = 0; i < num_events; i++) {
    process_socket(events_buffer[i].data.fd);
}`
    },
    interviewTips: [
      'Назовите epoll краеугольным камнем архитектуры Nginx и Node.js (библиотека libuv), позволившим решить классическую проблему C10K (обработка 10,000 соединений на одном сервере).'
    ],
    commonPitfalls: [
      'Думать, что epoll всегда быстрее select. При малом количестве сокетов (до 10-20) select может быть незначительно быстрее из-за отсутствия оверхеда на инициализацию дескриптора epoll.'
    ],
    tags: ['Linux', 'Kernel', 'Networking', 'Syscalls', 'Architecture']
  },
  {
    id: 'linux-51',
    title: 'Как работают механизмы безопасности ядра seccomp (secure computing mode) и AppArmor/SELinux?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'seccomp фильтрует системные вызовы процесса к ядру, запрещая опасные syscalls. AppArmor и SELinux реализуют мандатный контроль доступа (MAC) к файлам и сетям на основе профилей.',
    fullAnswer: `Для минимизации вектора атак (hardening) в Linux применяются политики ограничения прав на уровне ядра:

1. **seccomp (Secure Computing Mode)**:
   - Позволяет процессу в один конец ограничить доступные ему системные вызовы. Например, веб-сервер после старта и инициализации портов может вызвать \`seccomp\` и отключить себе системные вызовы вроде \`reboot\` или \`mount\`. Если злоумышленник взломает сервер и попробует выполнить запрещенный вызов, ядро мгновенно прибьет процесс сигналом SIGSYS.
   - Широко применяется в Kubernetes: поды запускаются с дефолтным профилем seccomp, блокирующим ~150 из ~350 системных вызовов ядра.

2. **AppArmor vs SELinux (Mandatory Access Control)**:
   - Классический Linux использует избирательный доступ (DAC) — владелец файла сам решает, кому дать права. Если процесс взломан под root, злоумышленник может всё. MAC-системы ограничивают действия процессов на основе политик безопасности, даже если они запущены от root.
   - **AppArmor** (Ubuntu/Debian): Работает на путях файлов. Простой синтаксис политик. Легко настраивается.
   - **SELinux** (RHEL/CentOS): Работает на метках (inode, сокеты, процессы). Крайне надежная, сложная комплексная система безопасности на базе ролей.`,
    codeSnippet: {
      language: 'json',
      code: `// Пример фрагмента seccomp профиля в Kubernetes (блокировка reboot)
{
  "defaultAction": "SCMP_ACT_ALLOW",
  "architectures": ["SCMP_ARCH_X86_64"],
  "syscalls": [
    {
      "names": ["reboot", "sysfs", "syslog"],
      "action": "SCMP_ACT_ERRNO"
    }
  ]
}`
    },
    interviewTips: [
      'Объясните, почему отключение SELinux (setenforce 0) в продакшене "чтобы заработало" — это критическая ошибка безопасности, выдающая незрелого инженера.'
    ],
    commonPitfalls: [
      'Блокировать системные вызовы через seccomp без тщательного тестирования приложения — ПО может упасть в непредсказуемый момент при обращении к стандартным библиотекам языка.'
    ],
    tags: ['Linux', 'Security', 'seccomp', 'SELinux', 'AppArmor']
  },
  {
    id: 'linux-52',
    title: 'Что такое HugePages (большие страницы памяти), как они настраиваются и зачем они нужны базам данных?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'HugePages увеличивает стандартный размер страницы виртуальной памяти с 4КБ до 2МБ или 1ГБ. Это снижает размер таблицы страниц процессора и минимизирует промахи в кэше TLB для тяжелых СУБД.',
    fullAnswer: `Процессор работает с виртуальной памятью, разбивая ее на страницы. Связку виртуальных адресов с физической RAM процессор кэширует в аппаратном буфере ассоциативной трансляции (**TLB - Translation Lookaside Buffer**).

**Проблема стандартных страниц (4 КБ)**:
Если у сервера 256 ГБ оперативной памяти под СУБД (PostgreSQL, Oracle), таблица страниц, описывающая адресное пространство, будет занимать гигабайты памяти (до 5-10 ГБ) в RAM. Это приводит к постоянному вымыванию записей из быстрого кэша TLB (TLB Misses), заставляя процессор тратить время на поиск адресов в медленной ОЗУ.

**Решение — HugePages**:
- Увеличивают размер страницы до 2 МБ (или 1 ГБ). Таблица страниц сокращается в 500 раз, легко помещаясь в кэш TLB.
- Память HugePages резервируется ядром на старте монолитным куском, никогда не сбрасывается в Swap и защищена от дефрагментации.

**Опасность Transparent HugePages (THP)**:
- THP — механизм динамического выделения больших страниц ядром на лету. Работает плохо для баз данных с интенсивной случайной записью (приводит к блокировкам и диким лагам аллокаций - Latency Spikes).
- **Рекомендация**: THP отключают, а HugePages настраивают строго статически.`,
    codeSnippet: {
      language: 'bash',
      code: `# Проверить текущее состояние HugePages в системе
cat /proc/meminfo | grep -i huge

# Зарезервировать 1024 страницы размером 2МБ (итого 2ГБ памяти)
sudo sysctl -w vm.nr_hugepages=1024

# Полностью отключить Transparent HugePages (рекомендация для Postgres)
echo "never" | sudo tee /sys/kernel/mm/transparent_hugepage/enabled`
    },
    interviewTips: [
      'Настройка HugePages — это высший пилотаж тюнинга серверов СУБД. Упомяните, что при включении HugePages в Linux необходимо также настроить параметр "huge_pages = on" в postgresql.conf.'
    ],
    commonPitfalls: [
      'Включать HugePages в ОС, но не настраивать лимиты разделяемой памяти (shared_buffers) в СУБД. Резерв памяти будет простаивать, а базе не хватит RAM.'
    ],
    tags: ['Linux', 'Memory', 'Kernel', 'PostgreSQL', 'Performance']
  },
  {
    id: 'linux-53',
    title: 'Как работает механизм Out Of Memory (OOM) Killer в Linux? Как ядро выбирает жертву и как этим управлять?',
    category: 'linux',
    difficulty: 'Senior',
    summaryAnswer: 'OOM Killer — механизм ядра Linux, который при критической нехватке оперативной памяти убивает один или несколько процессов для спасения ОС. Он выбирает жертву на основе очков badness (OOM Score), которые зависят от потребления RAM и приоритета процесса.',
    fullAnswer: `Когда оперативная память и Swap полностью заполняются, ядро Linux не может выделить память для системных нужд и во избежание полного краха ОС (Kernel Panic) запускает алгоритм **Out Of Memory Killer**.

**Как рассчитывается OOM Score**:
Каждому процессу рассчитывается оценка \`oom_score\` от 0 до 1000 (чем выше, тем больше шансов быть убитым):
1. **Базовый процент RAM**: процесс, использующий больше всего памяти (физической RSS + часть Swap), получает наибольшее количество базовых очков.
2. **Права процесса**: root-процессам оценка немного снижается (на 30 очков), так как они считаются системно важными.
3. **Время работы (возраст)**: старые запущенные процессы имеют чуть меньший приоритет на удаление, так как они, скорее всего, стабильны и важны.
4. **OOM Score Adj (Корректировка)**: пользовательский весовой коэффициент от -1000 до +1000, который вручную задает приоритет процесса. Значение -1000 полностью запрещает OOM Killer трогать этот процесс.

**Как управлять OOM поведением**:
- **Ручная настройка**: Изменение файла \`/proc/[PID]/oom_score_adj\`.
- **Systemd службы**: Настройка параметра \`OOMScoreAdjust=-900\` в systemd-юните, чтобы снизить вероятность убийства демона (например, sshd или nginx).
- **Overcommit (vm.overcommit_memory)**: параметр ядра, определяющий политику выделения памяти "авансом". При значении 2 ядро не выделяет больше памяти, чем \`Swap + (RAM * ratio)\`, что предотвращает запуск OOM Killer, но новые процессы будут получать ошибки аллокации памяти (\`ENOMEM\`).`,
    codeSnippet: {
      language: 'bash',
      code: `# Посмотреть топ процессов по вероятности убийства OOM Killer:
printf "PID\\tOOM_Score\\tCommand\\n" && for proc in [0-9]*; do [ -f /proc/\$proc/oom_score ] && printf "\$proc\\t\$(cat /proc/\$proc/oom_score)\\t\$(cat /proc/\$proc/cmdline | tr '\\0' ' ')\\n"; done | sort -k2 -nr | head -n 10

# Полностью защитить критический процесс (например, базы данных) от OOM Killer:
echo -1000 > /proc/1234/oom_score_adj`
    },
    interviewTips: [
      'Расскажите, что в контейнеризованных средах (Docker/Kubernetes) лимиты ресурсов памяти управляются через cgroups. OOM Killer на уровне контейнера убьет конкретный процесс внутри пода, если тот превысит memory limit, не затрагивая хост.'
    ],
    commonPitfalls: [
      'Защищать тяжелые уязвимые к утечкам процессы (например, кастомные скрипты) значением -1000. В случае бесконечной утечки памяти это приведет к падению системных демонов ОС или перезагрузке всего сервера.'
    ],
    tags: ['Linux', 'Memory', 'Kernel', 'OOM Killer', 'Troubleshooting']
  },
  {
    id: 'linux-54',
    title: 'Что такое inode? Что делать, если df показывает 50% свободного места, но ОС выдает ошибку «No space left on device»?',
    category: 'linux',
    difficulty: 'Middle',
    summaryAnswer: 'inode (index node) — это структура данных файловой системы, хранящая метаданные о файле (кроме имени и содержимого). Ошибка возникает, когда исчерпан лимит inodes (например, из-за миллионов микроскопических файлов/сессий), хотя физическое место на диске еще есть.',
    fullAnswer: `Файловая система в Linux (ext4, XFS) делит дисковое пространство на блоки данных (где лежит само содержимое файлов) и таблицу дескрипторов — **inodes (индексные дескрипторы)**.

**Что хранится в inode**:
- Размер файла.
- ID владельца (UID) и группы (GID).
- Права доступа (permissions).
- Временные метки (atime, mtime, ctime).
- Указатели на физические блоки диска, в которых лежат сами данные.
*Имя файла в inode не хранится* — оно хранится в структуре каталога (директории), которая связывает имя файла с номером его inode.

**Проблема исчерпания inodes**:
Каждая файловая система при форматировании получает фиксированное или расчетное количество inodes. Один файл или одна папка/симлинк занимают ровно один inode.
Если ваше приложение генерирует миллионы крошечных файлов (например, сессии PHP, файлы кэша, мелкие картинки или неразобранная очередь почтового сервера):
1. Вы забиваете таблицу inodes до 100%.
2. При попытке создать любой новый файл операционная система возвращает ошибку "No space left on device" (Недостаточно места на устройстве).
3. При этом утилита \`df -h\` показывает, что гигабайты памяти на диске свободны.

**Как решать**:
- Найти папку с миллионами мелких файлов с помощью скрипта.
- Удалить ненужные файлы. Внимание: обычная команда \`rm -rf *\` в такой папке упадет с ошибкой "Argument list too long". Удалять нужно через утилиту \`find . -type f -delete\` или \`rsync --delete\`.`,
    codeSnippet: {
      language: 'bash',
      code: `# Проверить использование inodes во всей системе:
df -i

# Найти топ-10 папок с наибольшим количеством файлов (подсчет inodes):
find / -xdev -type d -print | while read dir; do echo "$(find "$dir" -maxdepth 1 | wc -l) $dir"; done | sort -rn | head -10`
    },
    interviewTips: [
      'Укажите, что утилита lsof может помочь, если inodes/место заняты удаленным файлом, который все еще удерживается открытым каким-то запущенным процессом (так называемый deleted, but open file).'
    ],
    commonPitfalls: [
      'Пытаться отформатировать диск под миллионы файлов со стандартными параметрами ext4. Для таких сценариев нужно явно указывать ключ "-N" при создании ФС для увеличения количества inodes, либо использовать файловые системы с динамическими inodes, такие как XFS.'
    ],
    tags: ['Linux', 'FileSystem', 'Inodes', 'Troubleshooting', 'ext4']
  }
];
